# Changelog

Notable changes to this project. The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). The bump policy and pre-release roadmap are documented in [VERSIONING.md](VERSIONING.md).

Per-slice detail lives in [docs/changelog/slice-NNN.md](docs/changelog/) — the live file below carries only a compact pointer per slice (one headline + one-sentence summary) so the file stays bounded regardless of project age. Convention adopted in slice 628.

## Unreleased

**Docs (slice 896): CHANGELOG eviction — archive Unreleased pointers 855-860**
The live CHANGELOG reached ~57.7 KB (the `doc-size` audit ceiling is 60 KB), so the oldest six Unreleased pointers (slices 855-860) were evicted to a new per-cohort archive [docs/changelog/archive-slices-855-860.md](docs/changelog/archive-slices-855-860.md) — the same release-eviction pattern as the 849-854 eviction in slice 889. The live "Earlier Unreleased slices" pointer block extended to 778-860 (+ the new archive link); the [changelog README](docs/changelog/README.md) cohort index updated. Each evicted slice's full `slice-NNN.md` detail is untouched. Restores ~8 KB of headroom. Doc-only; `doc-size` + `doc-links` green.
Detail: [slice-896.md](docs/changelog/slice-896.md).

**Feat (slice 895): Chromatic Orb's leap — closes `chromatic-orb-no-leap` (Area 2 fully closed)**
RAW: a hit whose 3d8 show 2+ equal values makes the orb "leap to a different target of your choice within 30 ft" for a fresh attack + damage roll, up to `slotLevel` leaps, each creature once. Fix: new opt-in `leapsOnMatchingDamageDice` attack-mechanic flag (on chromatic-orb) + `CastSpellIntent.leapTargetIds` (consumer-supplied, the within-30-ft choice being positional). The `planAttackMechanic` spell-attack loop became a target **queue** — a hit with 2+ matching base d8s appends the next leap target; budget `maxLeaps = slotLevel`, each creature targeted once. With the flag off the queue is exactly the base targets (byte-unchanged). **Pattern-check**: the `for→while`-queue refactor first broke multi-beam spells (Eldritch Blast = several beams at one target); fixed by scoping the each-creature-once dedup to leap-target selection only. New 4-test slice-895 (leap on match; no leap when distinct; graceful without targets; slot-1 caps at 1 leap). No count change. `tsc` clean; `test:fast` (667 files, 4945 passed); doc-counts/size/links green. **Area 2 (Spell mechanics L0-4) is fully closed** — areas 1, 2, 4, 6, 7 now done.
Detail: [slice-895.md](docs/changelog/slice-895.md).

**Content (slice 894): Giant Insect (the 2024 summon) — closes `l4-giant-insect`**
The audit row's "transforms vermin into giant versions" is the **2014** spell; the SRD 5.2.1 Giant Insect is a Conjuration **summon** ("You summon a giant centipede, spider, or wasp ... uses the Giant Insect stat block ... disappears when it drops to 0 HP or when the spell ends"). So it wires as a `summon` mechanic reusing the shipped primitive (Conjure Animals / Summon Beast path): `{ ac: 15 (11+level), hpBase: 30, hpPerSlotAbove: 10, baseSlotLevel: 4, speedFeet: 40 }`, concentration-bound — a `CompanionSummoned` controlled by the caster. The form choice + form-specific attacks (Poison Jab / Web Bolt / Venomous Spew) + initiative-sharing are consumer-driven, as for every summon. Content only — no engine/schema change. New 3-test slice-894 (L4 summon HP 30 / AC 15 / caster-controlled; +10 HP per slot above 4; concentration-bound). Counts: L4 `25→26` wired / `3→2` deferred; cross-doc `216→217` wired / `55→54` schema-only (reconciled in gaps-spells / README / status / getting-started). `tsc` clean; `test:fast` (666 files, 4941 passed); doc-counts/size/links green. **The Area-2 frontier is now a single quirk, `chromatic-orb-no-leap`.**
Detail: [slice-894.md](docs/changelog/slice-894.md).

**Feat (slice 893): Confusion's behavior table + no Bonus Actions/Reactions — closes `confusion-table-not-rolled`**
RAW: a confused creature "can't take Bonus Actions or Reactions and must roll 1d10 at the start of each of its turns to determine its behavior." `confused-active` already had the WIS save-on-cast + end-of-turn save-ends; the two missing arms are now wired. (a) **No Bonus Actions/Reactions** — engine-enforced in `applyActionEconomyConsumed` via an `isConfused` gate mirroring the Slow-spell pattern (the `action` kind stays unblocked — the table gates which action, not whether). (b) **The 1d10 behavior table** — `engine.plan.rollConfusionBehavior` rolls the d10 (+ a 1d4 direction on a 1) deterministically and emits a notification-only `ConfusionBehaviorRolled` event with the bucket (`random-move`/`do-nothing`/`melee-random`/`normal`); the consumer executes the positional outcome (forced move / melee on a random in-reach creature) and respects the action-gate. New 6-test slice-893 (the gate throws for Bonus Action + Reaction but not Action; every d10 maps to its RAW bucket across 40 seeds; determinism; non-confused throws). No count change. `tsc` clean; `test:fast` (665 files, 4937 passed); doc-counts/size/links/planner-wiring green. The Area-2 frontier is now just `l4-giant-insect` + `chromatic-orb-no-leap`.
Detail: [slice-893.md](docs/changelog/slice-893.md).

**Docs (slice 892): `l4-locate-creature` + `l4-hallucinatory-terrain` are NOT A BUG — out of engine scope**
Both L4 spells ship `mechanicalEffects: []`; the audit asked whether that's a gap. It isn't — both are inherently DM-narrative. Locate Creature senses the *direction* to a creature within 1,000 ft, which needs world geography + creature locations beyond the encounter grid (deliberately not modeled — engine-scope.md); Hallucinatory Terrain is a 24-hour terrain illusion with an Investigation check to disbelieve (terrain appearance + adjudication are DM-owned). No engine-modelable mechanical combat effect, so `mechanicalEffects: []` is correct and an expert wouldn't expect the engine to resolve them — out of scope, not a deferred primitive. Refined the two `spell-coverage` skip reasons to say so (340 tests green); struck both rows `~~QUIRK~~ → NOT A BUG (out of scope)`. Rollup: Area 2 `5→3` open / `19→21` closed; Total `27→25` open / `90→92` closed / `0/9/18 → 0/9/16`. The Area-2 frontier is now the three genuinely-mechanical rows (`l4-giant-insect`, `confusion-table-not-rolled`, `chromatic-orb-no-leap`). Doc + test-reason only; no source change. `tsc`/`test:fast`-unaffected; doc-counts/size/links green.
Detail: [slice-892.md](docs/changelog/slice-892.md).

**Feat (slice 891): Ammunition consumption + recovery — closes `ammunition-not-consumed` (Area 6 fully closed)**
RAW (Ammunition property): a ranged Ammunition weapon fires "only if you have ammunition," expends one piece per shot, and you "recover half the ammunition you used" after a fight. The engine neither consumed nor required ammo. Fix (opt-in seam): new `AttackIntent.ammunitionInstanceId` — a ranged Ammunition-weapon shot appends an `AmmunitionQuantityChanged{delta:-1}` event to the attack tail (the Loading-weapon `WeaponLoaded` slot); the reducer decrements the stack, retiring it at 0, and a shot with a depleted/absent stack throws ("no ammunition"). New `engine.plan.recoverAmmunition({spent})` restores `floor(spent/2)` via the same event (consumer-driven downtime). Opt-in: with no `ammunitionInstanceId` the engine tracks/requires no ammo (byte-unchanged). Deferred: the ammo-type-matches-weapon check + free-hand-to-load. New 5-test slice-891; `recoverAmmunition` allowlisted in planner-wiring. **Area 6 (Base equipment mechanics) is fully closed.** No count change. `tsc` clean; `test:fast` (664 files, 4931 passed); doc-counts/size/links green.
Detail: [slice-891.md](docs/changelog/slice-891.md).

**Feat (slice 890): multiclass-entry proficiencies — closes `multiclass-entry-proficiencies`**
RAW 2024 ("As a Multiclass Character"): a class taken as a multiclass entry grants only a reduced proficiency subset (Fighter → Martial + Light/Medium/Shield, NOT Heavy), and NO class grants saving-throw proficiencies on multiclass. The three proficiency derivations (`isArmorTrained` / `isWeaponProficient` / save) walked every class and granted its FULL set, so a Wizard/Fighter over-granted Fighter's Heavy armor + STR/CON saves. Fix: `character.classes[0]` is the origin class (full proficiencies — the consumer orders the array; same snapshot contract as the slice-810 prereq validator); entries 1+ use a new per-class `multiclassProficiencies { armor, weapon }` reduced set (authored for 9 classes; Wizard/Monk/Sorcerer use the empty default), and saves come only from `classes[0]`. Skill/tool choices on multiclass stay consumer-resolved at build time. New 4-test slice-890; single-class characters byte-unchanged. Fallout verified low (no golden has a genuine multiclass character; the multiclass property suite asserts slot math + prof bonus, not proficiencies). No count change. `tsc` clean; `test:fast` (663 files, 4926 passed); doc-counts/size/links green.
Detail: [slice-890.md](docs/changelog/slice-890.md).

**Docs (slice 889): CHANGELOG eviction — archive Unreleased pointers 849-854**
The live CHANGELOG reached ~59 KB (the `doc-size` audit ceiling is 60 KB), so the oldest six Unreleased pointers (slices 849-854) were evicted to a new per-cohort archive [docs/changelog/archive-slices-849-854.md](docs/changelog/archive-slices-849-854.md) — the same release-eviction pattern as the 843-848 eviction in slice 881. The live "Earlier Unreleased slices" pointer block extended to 778-854 (+ the new archive link); the [changelog README](docs/changelog/README.md) cohort index updated. Each evicted slice's full `slice-NNN.md` detail is untouched. Restores ~8 KB of headroom. Doc-only; `doc-size` + `doc-links` green.
Detail: [slice-889.md](docs/changelog/slice-889.md).

**Content (slice 888): the Burning environmental Hazard — closes `no-environmental-hazards`**
RAW (rules-glossary "Burning" Hazard): "A burning creature or object takes 1d4 Fire damage at the start of each of its turns." New generic `burning` condition carrying `recurringDamage{1d4 fire, turnStart}` (no autoExpiry — it persists until the consumer removes it; the RAW Prone-and-roll / doused / submerged extinguish is narrative). `effects: []` — the recurringDamage is the whole behavior (the `acid-arrow-burning` shape); content only, reusing the slice-825 primitive. The row's other hazards are out of the engine's core scope: Dehydration/Malnutrition are per-**day** downtime hazards (the reversible Exhaustion core is expressible via the slice-887 counter pattern + the existing CON-save machinery, but the per-day cadence + food/water tracking is the consumer's calendar), and Extreme Cold/Heat is a Gameplay-Toolbox variant. New 3-test slice-888 (shape; a tick deals 1d4 fire; persists across ticks). +1 condition (176→177; rider 161→162). `tsc` clean; `test:fast` (662 files, 4922 passed); doc-counts/size/links green.
Detail: [slice-888.md](docs/changelog/slice-888.md).

**Feat (slice 887): Suffocation — the breath / Exhaustion mechanic — closes `no-suffocation`**
RAW 2024 (rules-glossary "Suffocation"): a suffocating creature "gains 1 Exhaustion level at the end of each of its turns. When a creature can breathe again, it removes all levels of Exhaustion it gained from suffocating." The engine owns the Exhaustion accounting; the "is it suffocating" trigger + the 1+CON-min breath-hold are the consumer's environmental model. New consumer-driven `engine.plan.tickSuffocation` (+1 Exhaustion/turn-end, recorded against the new reversible `Character.suffocationExhaustionLevels` counter) + `engine.plan.recoverFromBreath` (undoes exactly the suffocation-sourced levels, leaving other Exhaustion intact), both via a new `SuffocationExhaustionChanged` event whose reducer mirrors `applyExhaustionChanged` (from-level invariant + lethal-at-6 kill). Reaching Exhaustion 6 by suffocating is fatal. New 6-test slice-887 (accrue/accumulate/recover-exactly/preserve-other-exhaustion/no-op-recovery/fatal-at-6). New optional Character field (additive, old saves load clean). No count change. `tsc` clean; `test:fast` (661 files, 4919 passed); doc-counts/size/links green.
Detail: [slice-887.md](docs/changelog/slice-887.md).

**Feat (slice 886): the general Unseen Attackers and Targets rule — closes `unseen-attacker-general-rule`**
RAW: attacking a target you can't see → Disadvantage; a creature that can't see you → you have Advantage. The engine produced these only via the Invisible *condition* — no channel for darkness / obscurement / Blinded. Fix: new consumer LoS fact `AttackIntent.attackerCanSeeTarget` (`false` → Disadvantage) + the existing `targetCanSeeAttacker` now also drives the Advantage arm (`false` → Advantage, on top of its Dodge-bypass role, so it's suppressed by Elusive). Both opt-in (undefined → no change, every attack/golden byte-unchanged); fold into the 2024 cancellation (mutual blindness → straight roll). New 5-test slice-886; the dodge-los-gate `targetCanSeeAttacker=false` case updated from `'none'` to `'advantage'` (RAW: a dodging target that can't see the attacker grants Advantage). Deferred: threading the facts through the cast-spell spell-attack path (separate seam). Registered the fact slots in engine-scope/starter-pack-gaps/api-overview. No count change. `tsc` clean; `test:fast` (660 files, 4913 passed); doc-counts/size/links green.
Detail: [slice-886.md](docs/changelog/slice-886.md).

**Feat (slice 885): per-target Cover on spell saving throws — closes `aoe-save-ignores-cover`**
RAW (Cover): Half Cover grants +2, Three-Quarters +5, to AC **and Dexterity saving throws**. Single-target `rollSaveAgainstDC` sites honored cover (slice 550), but the cast-spell AoE/save block — every spell save's path — had no cover channel, so a creature behind half cover got no +2 Dex save vs Fireball. Fix: new consumer-supplied `CastSpellIntent.coverByTargetId` (per-target, since AoE targets differ; positions are consumer state, the seam like `lightLevel`/`aim`) applies `coverDexSaveBonus` to each Dex spell save with a breakdown entry, mirroring the single-target path; threaded into the unwilling-save site too. The **Sacred Flame** RAW exception ("gains no benefit from Cover") is modeled by a new `saveIgnoresCover` save-mechanic flag (set on sacred-flame, the only cover-ignoring spell) so adding cover doesn't introduce a new divergence. New 4-test slice-885 (same-seed isolation: half +2, three-quarters +5, per-target granularity, Sacred Flame exempt). Registered the fact slot in engine-scope/starter-pack-gaps/api-overview. No count change. `tsc` clean; `test:fast` (659 files, 4908 passed); doc-counts/size/links green.
Detail: [slice-885.md](docs/changelog/slice-885.md).

**Docs (slice 884): reconcile the already-closed `alert-initiative-swap` Area-5 row**
The Area-5 row claimed Alert's swap-initiative-with-an-ally arm "isn't [wired] (likely intentional)." Stale — **slice 468 wired both Alert arms.** The swap is a full `engine.plan.swapInitiative` → `InitiativeSwapped` planner/event/reducer (`planSwapInitiative` / `applyInitiativeSwapped`) that exchanges the two combatants' initiatives and recomputes `initiativeOrder`, with constraints: swapper has Alert, `planning` status only (RAW "immediately after you roll Initiative"), no self-swap, neither Incapacitated; willingness is consumer-asserted. Guarded by `slice-468-alert.test.ts` (9 tests; 7 cover the swap + rejections). Struck the row; Rollup: Area 5 `3→2` open / `8→9` closed / `0/1/2 → 0/1/1`; Total `34→33` open / `83→84` closed / `0/13/21 → 0/13/20`. Same shape as the slice-868/877 cross-ref reconciles. Docs only — slice 468 did the work. `doc-size` + `doc-links` + `doc-counts` green.
Detail: [slice-884.md](docs/changelog/slice-884.md).

**Docs (slice 883): `ki-sorcery-point-undercount` is NOT A BUG — per-level Focus/Sorcery counts verified at L6-7**
The audit's Area-5 `[verify]` row suspected a possible off-milestone undercount of Monk Focus (Ki) / Sorcerer Sorcery Points at L6-7. It's correct: the pack grants `ki` and `sorcery-points` with `max: { kind: 'level', classId }`, and `seedResourcesFromContent` evaluates that formula against the character's **current** class level — so a freshly-seeded L6 Monk has 6 Focus and an L7 Sorcerer has 7 Sorcery Points (RAW "Your Monk level determines the number of [Focus] points"). A per-level formula, not a frozen milestone. New 3-test slice-883 guard pins L2/L6/L7 for both classes (a milestone-freeze regression would fail). Marked `~~QUIRK~~ → NOT A BUG` (the savage-attacker / half-caster-l1-slot treatment). (Separate by-design note: the level-up reducer doesn't auto-mutate `resources[].max` — resources are consumer-seeded since slice 675.) Doc + guard only; no source change. Rollup: Area 5 `4→3` open / `7→8` closed; Total `35→34` open / `82→83` closed / `0/13/22 → 0/13/21`. `tsc` clean; doc-counts/size/links green.
Detail: [slice-883.md](docs/changelog/slice-883.md).

**Feat (slice 882): falling damage is rolled, not averaged — closes `falling-averaged-not-rolled`**
RAW (Falling): 1d6 Bludgeoning per 10 ft fallen, max 20d6 — **rolled**. `planFalling` substituted the fixed average (`round(dice×3.5)`), the lone averaged damage source in the engine. The planner already received the plan/commit `rng` (for the Concentration check), so the fix is local: a new `rollFallingDamage` rolls `1d6` per 10 ft (cap 20d6) via `rollDie(6, rng, 'damage')` — deterministic under replay, varies by seed; a sub-10-ft fall returns before drawing RNG (byte-identical). Slow Fall still subtracts `5×monk level` from the rolled total. New 5-test slice-882 (bounds, seed variance, the 20d6 cap, determinism, the no-dice case); the slow-fall test's three exact-average assertions became roll-aware (the 200-ft case compares a same-seed no-Slow-Fall roll to prove the −20 reduction). Regenerated the `s14-environmental` + `showcase` goldens (the fall damage + the downstream RNG-stream shift). No count change. `tsc` clean; `test:fast` (657 files, 4901 passed); doc-counts/size/links green.
Detail: [slice-882.md](docs/changelog/slice-882.md).

**Docs (slice 881): CHANGELOG eviction — archive Unreleased pointers 843-848**
The live CHANGELOG reached 58.3 KB (the `doc-size` audit ceiling is 60 KB), so the oldest six Unreleased pointers (slices 843-848) were evicted to a new per-cohort archive [docs/changelog/archive-slices-843-848.md](docs/changelog/archive-slices-843-848.md) — the slice-812 release-eviction pattern applied to un-tagged pointers, same as the 837-842 eviction in slice 874. The live "Earlier Unreleased slices" pointer block extended to 778-848 (+ the new archive link); the [changelog README](docs/changelog/README.md) cohort index updated. Each evicted slice's full `slice-NNN.md` detail is untouched. Restores ~8 KB of headroom (back to ~50 KB). Doc-only; `doc-size` + `doc-links` green.
Detail: [slice-881.md](docs/changelog/slice-881.md).

**Feat (slice 880): `no-hostility-model` — the ranged-in-melee hostility override (Area 4 fully closed)**
RAW (Ranged Attacks in Close Combat): a ranged attack is disadvantaged within 5 ft of a **hostile** creature. The engine has no hostility model, so its geometry fallback treated any adjacent creature as a threat — an archer next to a friendly cleric wrongly took disadvantage. The Pack-Tactics/flank arm already had a per-intent override (`attackerHasAllyAdjacentToTarget`, slice 445); the ranged-in-melee arm had none. Fix: new `attackerHasHostileAdjacent?: boolean` on `AttackIntent`/`ResolveAttackInput` — `rangedInMelee = input.attackerHasHostileAdjacent ?? positionDerived` (the symmetric mirror). A faction-aware consumer passes `false` (only a friendly adjacent → no disadvantage) or `true` (a hostile the engine can't see); `undefined` keeps the conservative geometry. With both geometry-blind arms now overridable, the row closes — **Area 4 (Core combat correctness) is fully closed.** New 5-test slice-880; registered the fact slot in engine-scope / starter-pack-gaps / api-overview. No count change. `tsc` clean; `test:fast` (656 files, 4896 passed); doc-counts/size/links green.
Detail: [slice-880.md](docs/changelog/slice-880.md).

**Feat (slice 879): Reaction reset timing — per-turn, not per-round — closes `reaction-reset-timing`**
RAW (rules-glossary "Reaction"): "Once you take a Reaction, you can't take another one **until the start of your next turn**." The engine reset `reactionUsedThisRound` for every combatant at `RoundEnded`, refreshing a spent reaction a beat too early for any combatant whose next turn isn't first in the new round (and ignoring initiative swaps / extra turns). Fix: the reset moved into `applyTurnStarted` (the active combatant), and `RoundEnded` no longer touches it. A spent reaction now persists through the round boundary until the reactor's own next turn; an extra turn carries its own `TurnStarted` reset. Safe to drop the round-end reset — every combatant gets exactly one `TurnStarted` per round (the "all have acted" invariant). The field keeps its historical name (per-turn semantics now, documented in-reducer). New 5-test slice-879 (recharge-at-own-turn; react-twice-in-a-round across the turn boundary; persists-through-RoundEnded; independent per-combatant timing; un-reacted stays available); the old action-economy "RoundEnded resets for everyone" test rewritten to the new semantics. No count change. `tsc` clean; `test:fast` (655 files, 4891 passed) incl. `fuzz-reactions-matrix` + combat property/golden tiers; doc-counts/size/links green.
Detail: [slice-879.md](docs/changelog/slice-879.md).

**Feat (slice 878): Frightened by multiple sources — the multi-source movement gate — closes `frightened-single-source-positional`**
RAW: a creature can be Frightened by more than one source at once, and "can't willingly move closer to the source of your fear" applies to **each**. Two compounding defects hid the second source: the condition reducer deduped Frightened by `conditionId` (so a second source's Frightened was never stored), and `planMove` read a single `.find()` (so a move toward a second source slipped through). Fix: the reducer dedup is now keyed by `(conditionId, sourceCharacterId)` — a same-source re-apply still dedupes, a distinct source stacks (safe: Frightened's LoS-gated disadvantage is per-bearer idempotent); and the move gate loops over **every** positioned fear source, naming the one it blocks on. Positionless / sourceless fear stays a deferred consumer/positional concern (RAW's "the source" needs a known map location). New 5-test slice-878 (stacks per source; same-source dedup; closer-to-neither; away-from-both allowed; single-source regression); the slice-581 source smoke check now accepts the `!== 'frightened'` continue-guard the refactor introduced. No count change (Frightened already wired; no new condition or effect kind). `tsc` clean; `test:fast` (654 files, 4886 passed) + doc-counts/size/links green.
Detail: [slice-878.md](docs/changelog/slice-878.md).

**Docs (slice 877): reconcile the already-closed `prone-cant-crawl` Area-4 row**
Slice 867 (climb/swim/crawl surcharge) added the `'crawl'` movement mode — it keeps the mover Prone (no stand-up) and charges the +1 ft/ft surcharge — which closed both the Area-8 `climb-swim-crawl-cost` row (struck in 867) and the Area-4 quirk `prone-cant-crawl` (its two gaps: "no crawl modality, and crawl's +1 ft/ft cost is unmodeled"), but the Area-4 row was left unstruck. Same cross-area miss as slice 868's exhaustion-6 reconcile. Struck the Area-4 row (pointing at the slice-867 closure); Rollup: Area 4 `4→3` open / `8→9` closed / `0/0/4→0/0/3`; Total `40→39` open / `77→78` closed / `0/13/27→0/13/26` (117 rows unchanged). Docs only — slice 867 did the work. `doc-size` + `doc-links` green.
Detail: [slice-877.md](docs/changelog/slice-877.md).

**Feat (slice 876): Shocking Grasp's "can't make Opportunity Attacks" rider — closes `shocking-grasp-no-oa-denial`**
RAW: a Shocking Grasp hit also bars the target's Opportunity Attacks until the start of its next turn. The damage was wired; the OA-denial wasn't. Reuses the Monk Open Hand "Addle" shape (no new effect kind): a new effect-less `shocking-grasped` marker applied via the attack mechanic's `conditionOnHit`, read by a shared `cannotMakeOpportunityAttack` guard (`OA_PREVENTING_CONDITIONS` = `addled` + `shocking-grasped`, composed with the action-blockers) at both the OA **emission** (`planMove`) and **resolution** (`planOpportunityAttack`). **Pattern-check**: `addled` was previously read only at the resolution, so an Addled creature was still *offered* an OA in the emission — folding it into the shared guard suppresses it there too. New 5-test slice-876 (the wire; a hit applies the marker; a positioned mover leaving reach doesn't offer the grasped reactor an OA while the free one gets one; the addled-emission pattern-check; the resolution rejection). +1 condition (`175→176`); no new effect kind; Shocking Grasp stays wired (missing-arm close). `tsc` clean; `test:fast` (653 files, 4881 passed) + pack-integrity + doc-counts/size/links green.
Detail: [slice-876.md](docs/changelog/slice-876.md).

**Feat (slice 875): Enlarge/Reduce weapon-damage rider — the `WeaponDamageDelta` primitive — closes `enlarge-reduce-no-damage-rider`**
RAW Enlarge adds 1d4 to the target's weapon/Unarmed-Strike damage on a hit; Reduce subtracts 1d4 (can't drop below 1). `enlarged-active`/`reduced-active` carried only the STR adv/disadv (slice 849) — no `AddModifier` (flat) or `AddDamage` (separate fixed-type component) fit "±1d4 to the weapon's own damage, floor 1." New **`WeaponDamageDelta { dice, mode: 'add' | 'subtract' }`** effect (EFFECT_KINDS `69→70`), read by the attack planner off the attacker's effect stack and applied to the bearer's own weapon damage of the weapon's type — the `HalvesStrengthWeaponDamage` (Ray of Enfeeblement) precedent. `enlarged-active` `{1d4, add}`, `reduced-active` `{1d4, subtract}` (a subtract floors the weapon component at 1). The delta die is rolled after the weapon dice, so a normal-size attacker draws no extra RNG (every existing attack byte-identical; at a fixed seed `sized = baseline ± delta`); the delta isn't crit-doubled (minor deferral). New 4-test slice-875 (the riders; +1d4 over baseline; 1–4 less; the never-below-1 floor on a dagger). Primitive count `69→70` / `70→71` reconciled across 7 docs (`release:doc-review` "primitive count 70 (+ Custom = 71) MATCHES"). **Both damage-pipeline primitives now closed.** `tsc` clean; `test:fast` (652 files, 4876 passed) + exports + doc-counts/size/links + `release:doc-review` green.
Detail: [slice-875.md](docs/changelog/slice-875.md).

**Feat (slice 874): Acid Arrow — half-on-miss + the delayed 2d4 drip — closes `acid-arrow-no-delayed-or-miss`**
RAW: a ranged spell attack dealing 4d4 Acid + 2d4 at the end of the target's next turn on a hit, and half the initial on a miss. The engine dealt flat 4d4 on hit (nothing on a miss, no delayed). Two arms: a new **`halfDamageOnMiss`** flag on the attack mechanic (the rolled initial is halved on a miss — the same outcome as the Evoker Potent-Cantrip half-on-miss path, now mechanic-declared at any level); and the delayed 2d4 reuses the slice-825 `recurringDamage` via **`conditionOnHit: 'acid-arrow-burning'`** (applied on a hit only — no delayed on a miss), a `recurringDamage{2d4 acid, turnEnd}` condition the consumer ticks once at the target's turn-end (bearer-keyed `autoExpiry` lifts it). The +1d4-per-slot upcast on the delayed die is deferred (static condition dice, as with the smites). New 3-test slice-874 (the wire + condition shape; a hit deals full 4d4 + applies the lingering condition, a miss deals half + no condition; the ticked drip is 2d4 Acid). New `acid-arrow-burning` condition (`173→… 174→175`; the wired-conditions snapshot is unchanged — it's effect-less, driven by recurringDamage). acid-arrow stays in the wired count (already wired). `tsc` clean; `test:fast` (651 files, 4872 passed) + doc-counts/size/links green.
Detail: [slice-874.md](docs/changelog/slice-874.md).

**Feat (slice 873): Guardian of Faith + the aura damage-budget primitive — closes `l4-guardian-of-faith`**
The spell shipped `mechanicalEffects: []`. RAW: an 8-hour (non-concentration) guardian deals 20 Radiant (DEX save half) to enemies within 10 ft and **vanishes once it has dealt 60 damage**. The damage is an `aura-damage` tick (10-ft, DEX, 20 radiant, half) via the slice-872 non-concentration path; the new piece is the **cumulative-damage budget**: a `damageBudget` on the mechanic, held as `auraDamageBudgetRemaining` on the spell's `SpellEffectStarted` EffectInstance (a budgeted aura now allocates one at cast — unbudgeted auras stay effect-less). `planTickAura` gains an `effectInstanceId` tick path that charges the post-mitigation damage dealt against the budget (a new `AuraDamageBudgetSpent` event), and emits `ConcentrationBroken('used')` — the guardian vanishes, deleting the effect — once it's spent its 60. Guardian's flat 20 is authored as `0d6+20` (0 dice + 20). Placement / invulnerability / membership stay consumer-managed. New 3-test slice-873 (the wire; cast claims no Concentration + seeds the 60 budget; each tick deals DEX-save radiant, spends exactly that much, vanishes at 60 with the effect deleted). `spell-coverage` → wired aura-damage. New event `AuraDamageBudgetSpent` (union/apply/transcript). Counts: wired `215→216` / schema-only `56→55` / L4 `24→25` / pct `~63%→~64%`. **The L4 summon/aura block is now closed.** `tsc` clean; `test:fast` (650 files, 4869 passed) + exports + migrations + doc-counts/size/links + `release:doc-review` green.
Detail: [slice-873.md](docs/changelog/slice-873.md).

**Feat (slice 872): Faithful Hound + the non-concentration aura tick — closes `l4-faithful-hound`**
The spell shipped `mechanicalEffects: []`. RAW: at the start of each of your turns the (8-hour, **non-concentration**) hound bites one enemy within 5 ft — DEX save or 4d8 Force. The bite is an `aura-damage` tick (5-ft, DEX, 4d8 Force, no half), the Spirit-Guardians/Wall-of-Fire machinery — but `planTickAura` resolved the aura's spell from `caster.concentrationEffectId`, so it only worked for concentration auras. `tickAura` now ticks a non-concentration aura by `spellId` (`TickAuraIntent.spellId` + optional `slotLevel`) — the planner reads the spell from content, no effect instance needed (the aura's position/existence is consumer-managed, as for every aura); the concentration path is byte-unchanged. **Pattern-check**: Grease (L1) — the other non-concentration aura-damage spell — had the same unusable tick (it was even `skip`-marked citing this gap) and now ticks via the same path, no content change. Faithful Hound's invisible-watchdog / alarm / placement arms stay consumer/narrative. New 4-test slice-872 (the wire; cast claims no Concentration + bite ticks by spellId, DEX DC 15 → 4d8 Force; no-spellId tick still needs concentration; the Grease pattern-check → Prone). `spell-coverage` gains a `nonConcentration` aura-damage flag; faithful-hound + grease flip to wired. Counts: wired `214→215` / schema-only `57→56` / L4 `23→24`. `tsc` clean; `test:fast` (649 files, 4865 passed) + doc-counts/size/links + `release:doc-review` green. No new condition/effect/snapshot.
Detail: [slice-872.md](docs/changelog/slice-872.md).

**Feat (slice 871): Aura of Life + the `recurringHeal` primitive — closes `l4-aura-of-life`**
The spell shipped `mechanicalEffects: []`. RAW: a 30-ft Emanation granting allies Necrotic Resistance + reviving 0-HP allies by 1 HP each turn (Concentration). Wired via the aura-as-condition pattern (like the Paladin auras): a `buff` mechanic applies the new `aura-of-life-active` condition (concentration-bound — the buff conditions clear via the `ConcentrationStarted` sweep when Concentration ends; aura membership is consumer-managed, the positional emanation). The condition carries `GrantResistance{necrotic}` (a necrotic hit on a bearer is halved) + the **new `recurringHeal` primitive** — the heal mirror of slice-825 `recurringDamage`: a condition declares `recurringHeal { amount, trigger, onlyAtZeroHp? }` and `engine.plan.tickRecurringHeal` emits a flat `Healed` when the consumer ticks it; `onlyAtZeroHp` gates it to a downed bearer (Aura of Life only revives the dying). Deferred: the "HP maximum can't be reduced" arm (no prevent-max-HP-reduction effect yet). New 4-test slice-871 (wire + condition shape; concentration-bound apply to chosen creatures; necrotic halving / fire untouched; tickRecurringHeal revives a 0-HP bearer by 1, no-op above 0). `spell-coverage` → `buff`; conditions `173→174`; spell-wired `213→214` / schema-only `58→57` / L4 `22→23`; coverage snapshot +1 condition. No new effect kind. `tsc` clean; `test:fast` (648 files, 4859 passed) + coverage/planner-wiring/doc-counts/size/links + `release:doc-review` green.
Detail: [slice-871.md](docs/changelog/slice-871.md).

**Feat (slice 870): Compulsion — multi-target WIS save → Charmed (Concentration) — closes `l4-compulsion`**
The spell shipped `mechanicalEffects: []` — a cast did nothing. RAW: "Each creature of your choice that you can see within range must succeed on a Wisdom saving throw or have the Charmed condition until the spell ends." Content-only, the Dominate/Banishment pattern: multi-target `{ save WIS, conditionOnFail: 'charmed' }`, concentration-bound, reusing the **shared `charmed`** (so the slice-807 can't-attack-the-charmer / social-Advantage arms apply). The Bonus-Action forced-movement direction ("designate a direction ... must use as much of its movement as possible to move that way") and its coupled "after moving in this way, a target repeats the save" are one **positional/consumer arm** (deferred — the engine doesn't model the directed forced move, and the re-save is triggered by it), the same shape as Dominate Beast's deferred control link. No engine change. New 3-test slice-870 (the wire; a save per chosen target → failed targets Charmed by the caster + concentration-bound; a successful saver unaffected). `spell-coverage` flips compulsion → wired (`save`); spell-wired counts bumped (L4 `21→22`, total `212→213`, schema-only `59→58`). `tsc` clean; `test:fast` (647 files, 4854 passed) + doc-counts/size/links + `release:doc-review` green.
Detail: [slice-870.md](docs/changelog/slice-870.md).

**Feat (slice 869): Dominate Beast — WIS save → Charmed (Concentration) + damage re-save — closes `l4-dominate-beast`**
The spell shipped `mechanicalEffects: []` — a cast did nothing. RAW: "One Beast ... must succeed on a Wisdom saving throw or have the Charmed condition ... Whenever the target takes damage, it repeats the save, ending the spell on itself on a success." Content-only wire reusing the **shared `charmed`** condition (so the slice-807 can't-attack-the-charmer / social-Advantage / Countercharm arms apply for free): `{ save WIS, targetCreatureType: 'Beast', conditionOnFail: 'charmed', conditionRepeatsSaveOnDamage: true }`, concentration-bound. One new opt-in save-mechanic field, `conditionRepeatsSaveOnDamage`, stamps the slice-388 per-instance recurring save (WIS vs the caster's spell DC, resolved at cast time) onto the applied `charmed`, so the consumer ticks `tickRecurringSave` on damage and a success ends it (the trigger-agnostic path Hideous Laughter already uses). **Pattern-check**: the same flag added to Dominate Person (L5) + Monster (L8), which carried the identical RAW clause. Deferred (DM/consumer): the telepathic control link, the "Advantage if you/allies are fighting it" arm, the upcast durations. New 4-test slice-869 (all three Dominate spells carry the flag; a Beast failing the WIS save is Charmed DC 15 w/ concentration + re-save stamp; a Humanoid is skipped; the ticked damage re-save ends the charm on a success). `spell-coverage` flips dominate-beast → wired; spell-wired counts bumped (L4 `20→21`, total `211→212`, schema-only `60→59`, `~62%→~63%`). No new condition/effect/primitive. `tsc` clean; `test:fast` (646 files, 4850 passed) + doc-counts/size/links + `release:doc-review` green.
Detail: [slice-869.md](docs/changelog/slice-869.md).

**Docs (slice 868): reconcile the already-closed `exhaustion-6-not-fatal` Area-8 cross-ref**
`exhaustion-6-not-fatal` (Exhaustion 6 = death) appears as a full row in both Area 4 (primary) and Area 8 (cross-ref). Slice 800 closed it (both exhaustion paths call `markCreatureDead` at `EXHAUSTION_MAX` — verified at `reducers/combat.ts:194,316`) and struck the Area-4 row, but left the Area-8 cross-ref unstruck. Since the Rollup counts every row once (`open + closed = 117`), that phantom row registered a non-existent open DIVERGENCE in Area 8. Struck the Area-8 row (pointing at the slice-800 closure); Rollup: Area 8 `6 → 5` open / `8 → 9` closed / `0/1/5 → 0/0/5` (now quirks-only, like Area 4); Total `49 → 48` open / `68 → 69` closed / `0/21/28 → 0/20/28` (117 rows unchanged). Docs only — slice 800 did the engine work; this makes the audit reflect it. `doc-size` + `doc-links` green.
Detail: [slice-868.md](docs/changelog/slice-868.md).

**Feat (slice 867): climb / swim / crawl movement-cost surcharge — closes `climb-swim-crawl-cost`**
RAW (Climbing / Swimming / Crawling): "each foot of movement costs 1 extra foot (2 extra feet in Difficult Terrain)." `planMove` accepted a `movementMode` but only `'fly'` did anything. It now charges a **+1 ft per geometric foot** surcharge for `'climb'` / `'swim'` / `'crawl'`. The Difficult-Terrain doubling falls out for free: the path cost already doubles Difficult-Terrain feet, so +1 per *geometric* foot reproduces the RAW total (normal 1+1=2/ft, difficult 2+1=3/ft); geometric distance = path step count (mapped) or Chebyshev (positionless). Climb/Swim **waive** the cost when the mover has the matching effective Climb/Swim Speed (`getEffectiveSpeedForMode > 0`); Crawl never waives. New `'crawl'` mode keeps the mover **Prone** — the RAW alternative to standing up, paying the +1 ft/ft surcharge instead of the half-speed stand-up (no `ConditionRemoved`). The surcharge counts against remaining movement (over-budget throws, naming it) and folds into `feetTraveled`. `MovementMode` gains `'crawl'` (snapshots track names, not union shape — unchanged). New 7-test slice-867 (walk base; climb/swim double without the matching speed; Giant Rat climb 30 exempt; Prone walk stands up + sheds Prone; Prone crawl stays Prone + crawl surcharge; over-budget climb throws). `tsc` clean; `test:fast` (645 files, 4846 passed — +1 file/+7 tests over 866, default walk/fly byte-identical, zero regression) + doc audits green.
Detail: [slice-867.md](docs/changelog/slice-867.md).

**Feat (slice 866): over-capacity Speed cap (≤ 5 ft) — closes `over-capacity-speed-5`**
RAW (Carrying Capacity): "While dragging, lifting, or pushing weight in excess of the maximum weight you can carry, your Speed can be no more than 5 feet." Slice 865 gave `computeEncumbrance` a binary `overCapacity` flag but nothing consumed it. `getEffectiveSpeedForMode` (the chokepoint behind every speed alias) now applies the cap as its last step: when carried inventory weight exceeds the size×STR×15 carry max, every mode's effective Speed is clamped to ≤ 5 ft (`Math.min(uncapped, 5)`). RAW "your Speed" is general → walk and the non-walk modes are all capped; the cap only lowers (a 0-set Grappled/Restrained stays 0, a `set` override like Phantom Steed clamps down). `GetEffectiveSpeedInput` already carries `{ character, content, itemInstances }`, so no signature change; an empty inventory takes a no-weigh fast path. The Boots of Striding and Springing exemption is deferred (the item ships inert). New 8-test slice-866 (walk capped over/at/under the max; the empty-inventory fast path; `getEffectiveSpeeds.walk`; an over-capacity Ghost's fly 40 → 5; an under-capacity flyer keeps fly 40; the only-lowers invariant). No new export/condition/effect/content. `tsc` clean; `test:fast` (644 files, 4839 passed — exactly +1 file/+8 tests over 865, zero golden cascade) + doc audits green.
Detail: [slice-866.md](docs/changelog/slice-866.md).

**Refactor (slice 865): 2024 Carrying Capacity model — size-scaled carry + binary over-capacity — closes `encumbrance-variant-2014` + `carry-capacity-size`**
RAW (SRD 5.2.1, Carrying Capacity) has NO 2014-style "encumbered"/"heavily encumbered" tiers: carry max = STR × 15 × size factor, drag/lift/push is double, and the *only* consequence of exceeding the carry max is "your Speed can be no more than 5 feet." Two audit rows collapse into one model fix. **`computeCarryingCapacity`** now scales the STR×15 base by size (Tiny ×0.5, Small/Medium ×1, Large ×2, Huge ×4, Gargantuan ×8, reading `sizeOverride ?? species.size`) and returns `{ capacity, pushDragLift, breakdown }` — Goliath Powerful Build still counts one size larger on top. **`computeEncumbrance`** drops the duplicated flat formula (now delegates to the single carry source) and the `EncumbranceLevel` tier type + `unencumbered`/`encumbered`/`heavily-encumbered` labels; its result is binary: `{ carriedWeight, carryCapacity, pushDragLiftCapacity, overCapacity }` where `overCapacity = carriedWeight > carryCapacity`. The Speed ≤ 5 consequence is the follow-up `over-capacity-speed-5` (slice 866). `EncumbranceLevel` removed from `src/index.ts` + the derive barrel (exports snapshot loses the one type — the only non-additive change; `EncumbranceResult` stays, reshaped). New 12-test slice-865 (size factors Tiny→Gargantuan; ×2 drag/lift/push; Powerful Build Medium→Large; the binary over/at/under-capacity flag with real musket weights). slice-582 + tabulated-math + character-sheet tests updated to the new shape; all stay green. `tsc` clean; `test:fast` (643 files, 4831 passed) + doc audits green.
Detail: [slice-865.md](docs/changelog/slice-865.md).

**Feat (slice 864): `validateAttunement` — attunement preconditions — closes `attune-prereq-not-validated`**
The content-less `applyItemAttuned` reducer gates the 3-slot limit + double-attunement but can't see whether an item even requires attunement, nor its restriction. Attunement is consumer-committed (no `planAttune`), so — the `validateMulticlass` pattern — the engine ships `validateAttunement(character, instance, content)` → `{ issues, unverifiedCondition? }`. `issues` are the machine-checkable blockers (item doesn't require attunement / already attuned / 3-slot limit full); `attunementCondition` ("by a Dwarf", "by a Spellcaster") is free-form prose the engine can't verify, returned as `unverifiedCondition` for the consumer/DM to confirm. New 5-test slice-864 (unrestricted item attunable; restricted item surfaces its condition; non-attunement item / already-attuned / slot-full all rejected). Exported `validateAttunement` + `MAX_ATTUNED_ITEMS` + `AttunementValidation` (additive exports snapshot); documented in api-overview. No reducer/behavior change to existing surfaces. `tsc` clean; `test:fast` (642 files, 4819 passed) + doc audits green.
Detail: [slice-864.md](docs/changelog/slice-864.md).

**Feat (slice 863): jump distances (`computeJumpDistances`) — closes `no-jump-distance`**
RAW Long/High jump formulas were unmodeled. New pure derive in `speed.ts`, exported: `computeJumpDistances({ character, content, itemInstances })` → `{ longJumpFeet, standingLongJumpFeet, highJumpFeet, standingHighJumpFeet }`. Long Jump = effective STR score; High Jump = `max(0, 3 + STR mod)`; each standing variant = ⌊half⌋. Reuses `GetEffectiveSpeedInput` and reads **effective** STR (effect-stack floor/increase + drain — the Graze/armor-speed score), so Gauntlets of Ogre Power lengthen the jump. RAW each foot of jump costs a foot of movement — a consumer/positional concern, so the engine reports distances only. New 3-test slice-863 (the four distances at STR 16 / 8; the High-Jump min-0 clamp at STR 3; the Gauntlets effective-STR read → STR 19 jumps). Exports snapshot gains `computeJumpDistances` + `JumpDistances` (additive); documented in api-overview. No behavior change to any existing surface. `tsc` clean; `test:fast` (641 files, 4814 passed) + doc audits green.
Detail: [slice-863.md](docs/changelog/slice-863.md).

**Fix (slice 862): a creature must have at least 1 HP to start a Long Rest — closes `rest-no-min-1hp`**
RAW (Long Rest): "To start a Long Rest, you must have at least 1 Hit Point." `planLongRest` applied the rest's benefits to every participant unconditionally, so a 0-HP / dying creature could long-rest straight to full. A gate at the top of the planner now throws (naming the creature) if any participant is below 1 HP — it must regain ≥1 HP first (a heal, or the stable-creature 1-HP-after-1d4-hours recovery). Explicit failure over the prior silent rest-to-full; the gate lives in the planner (not the reducer the row cited) so the rejection surfaces before any event commits. New 3-test slice-862 (0-HP participant throws; ≥1-HP rests normally; a party rest with one 0-HP member is rejected, naming them). Every existing long-rest test stays green. No content/snapshot change. `tsc` clean; `test:fast` (640 files, 4811 passed) + doc audits green.
Detail: [slice-862.md](docs/changelog/slice-862.md).

**Fix (slice 861): a creature that takes falling damage lands Prone — closes `falling-no-prone`**
RAW (Falling): "When the creature lands, it has the Prone condition unless it avoids taking any damage from the fall." `planFalling` applied the damage but never the Prone-on-landing. It now appends a `prone` ConditionApplied to the faller iff the fall dealt damage (the mitigated total > 0) — so Feather Fall (`GrantFallingProtection`), Slow Fall reducing to 0, and full Bludgeoning Immunity all leave the creature standing, while any fall that hurts knocks it Prone. The averaged-vs-rolled damage value (`falling-averaged-not-rolled`) is left unchanged (kept out to avoid the broad value-cascade a rolled-damage change pushes through every falling golden). New 2-test slice-861 (a damaging fall applies Prone after the DamageApplied; a Slow-Fall-to-0 fall returns `[]` with no Prone); the Slow Fall / Feather Fall / Death Ward unit tests stay green (they find events by type). Goldens regenerated: `s14-environmental` purely additive (a "is now Prone" line per fall); `showcase` a correct Prone-driven cascade (Vex attacks while Prone → Advantage cancelled, Sneak Attack drops → seeded-RNG cascade, replay-equivalent). No content/snapshot change. `tsc` clean; `test:fast` (639 files, 4808 passed) + doc audits green.
Detail: [slice-861.md](docs/changelog/slice-861.md).

**Earlier Unreleased slices (778-860)** — their per-slice pointers are archived in [docs/changelog/archive-slices-778-802.md](docs/changelog/archive-slices-778-802.md) (778-802), [docs/changelog/archive-slices-803-808.md](docs/changelog/archive-slices-803-808.md) (803-808), [docs/changelog/archive-slices-809-813.md](docs/changelog/archive-slices-809-813.md) (809-813), [docs/changelog/archive-slices-814-818.md](docs/changelog/archive-slices-814-818.md) (814-818), [docs/changelog/archive-slices-819-824.md](docs/changelog/archive-slices-819-824.md) (819-824), [docs/changelog/archive-slices-825-830.md](docs/changelog/archive-slices-825-830.md) (825-830), [docs/changelog/archive-slices-831-836.md](docs/changelog/archive-slices-831-836.md) (831-836), [docs/changelog/archive-slices-837-842.md](docs/changelog/archive-slices-837-842.md) (837-842), [docs/changelog/archive-slices-843-848.md](docs/changelog/archive-slices-843-848.md) (843-848), [docs/changelog/archive-slices-849-854.md](docs/changelog/archive-slices-849-854.md) (849-854), and [docs/changelog/archive-slices-855-860.md](docs/changelog/archive-slices-855-860.md) (855-860) to keep this file under the single-Read ceiling; full detail lives in the `slice-NNN.md` files. Still part of the active, un-tagged cycle (no release cut since `0.11.0-alpha.0`).

## Older releases

Tagged release `0.11.0-alpha.0` lives in [docs/changelog/released-versions-0.11.0-alpha.0.md](docs/changelog/released-versions-0.11.0-alpha.0.md); `0.10.0-alpha.0` lives in [docs/changelog/released-versions-0.10.0-alpha.0.md](docs/changelog/released-versions-0.10.0-alpha.0.md); `0.9.0-alpha.0` lives in [docs/changelog/released-versions-0.9.0-alpha.0.md](docs/changelog/released-versions-0.9.0-alpha.0.md); `0.8.0-alpha.0` lives in [docs/changelog/released-versions-0.8.0-alpha.0.md](docs/changelog/released-versions-0.8.0-alpha.0.md); `0.7.0-alpha.0` lives in [docs/changelog/released-versions-0.7.0-alpha.0.md](docs/changelog/released-versions-0.7.0-alpha.0.md); `0.6.0-alpha.0` lives in [docs/changelog/released-versions-0.6.0-alpha.0.md](docs/changelog/released-versions-0.6.0-alpha.0.md); `0.5.0-alpha.0` lives in [docs/changelog/released-versions-0.5.0-alpha.0.md](docs/changelog/released-versions-0.5.0-alpha.0.md); `0.4.0-alpha.0` lives in [docs/changelog/released-versions-0.4.0-alpha.0.md](docs/changelog/released-versions-0.4.0-alpha.0.md); `0.3.0-alpha.0` lives in [docs/changelog/released-versions-0.3.0-alpha.0.md](docs/changelog/released-versions-0.3.0-alpha.0.md); `0.2.0-alpha.0` lives in [docs/changelog/released-versions-0.2.0-alpha.0.md](docs/changelog/released-versions-0.2.0-alpha.0.md); `0.1.0-alpha.15` lives in [docs/changelog/released-versions-alpha-15.md](docs/changelog/released-versions-alpha-15.md); `0.1.0-alpha.14` lives in [docs/changelog/released-versions-alpha-14.md](docs/changelog/released-versions-alpha-14.md); `0.1.0-alpha.6` through `0.1.0-alpha.13` live in [docs/changelog/released-versions-alpha-6-13.md](docs/changelog/released-versions-alpha-6-13.md); `0.1.0-alpha.0` through `0.1.0-alpha.5` (the pre-rename `ttrpg-engine-dnd` package, all unpublished from npm in May 2026 on IP-cleanup grounds) live in [docs/changelog/released-versions.md](docs/changelog/released-versions.md). Per-cohort slice-detail archives are indexed in [docs/changelog/README.md](docs/changelog/README.md).
