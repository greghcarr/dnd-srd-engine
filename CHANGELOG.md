# Changelog

Notable changes to this project. The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). The bump policy and pre-release roadmap are documented in [VERSIONING.md](VERSIONING.md).

Per-slice detail lives in [docs/changelog/slice-NNN.md](docs/changelog/) — the live file below carries only a compact pointer per slice (one headline + one-sentence summary) so the file stays bounded regardless of project age. Convention adopted in slice 628.

## Unreleased

**Docs (slice 837): `weapon-material-qualifier` is NOT A BUG — stale 2014 finding**
The L7 audit's "`GrantResistance.qualifier` needs silvered/adamantine" row was 2014-based. Canon-verifying against `references/srd-markdown/`: SRD 5.2.1 **removed the weapon-material resistance qualifier entirely** — monster resistances are flat typed lists (Chain Devil: "Bludgeoning, Cold, Piercing, Slashing"; Clay Golem: "Bludgeoning, Piercing, Slashing"), the Werewolf has no Resistances line and the Stone Golem only Poison/Psychic immunity (the 2014 "nonmagical not silvered/adamantine" arm is gone). "Silvered" isn't a 2024 mechanic; "adamantine" is only an object-AC value + the Adamantine Armor crit rule. The pack already models every relevant creature with the flat 2024 resistance, and zero monsters use the qualifier — implementing silvered/adamantine would be edition drift. The row is struck through (NOT A BUG) and moved to "Confirmed correct / by-design"; a new 4-test `slice-837` guard pins the named creatures' flat resistances + the no-monster-uses-the-qualifier invariant. Doc + test only; no source change. `tsc` clean; guard + doc audits green.
Detail: [slice-837.md](docs/changelog/slice-837.md).

**Feat (slice 836): ooze Split (Black Pudding / Ochre Jelly) — closes `ooze-split-on-damage`**
The oozes' Split was inert — and its response (half the *current* HP, one size smaller, original replaced) is beyond the fixed-HP/same-size `SpawnCreature` action. New statblock **`split`** spec (`{ damageTypes, minHp }`) + consumer-driven **`engine.plan.oozeSplit`**: the consumer detects the trigger (slashing/lightning damage or Bloodied — it has the DamageApplied events + HP) and calls it; the engine validates RAW eligibility (Large/Medium, ≥10 HP) and emits two `CharacterCreated` of the same statblock one size smaller (a `sizeOverride`) at `floor(currentHP/2)` each + `CreatureDestroyed` on the original (it's replaced). Placement + the "acts on its Initiative" insertion stay consumer-managed (positions/encounter are out of engine scope, like the Wraith/Wight spawns). The 35-field spawn snapshot was extracted from the Troll's `fireSpawnCreature` into a shared `buildSpawnedCharacter` (rule-of-two; the Troll path stays byte-compatible). Content: `split` on Black Pudding (CR 4) + Ochre Jelly (CR 2). New 5-test slice-836. No new condition/event/effect kind/weapon → no doc-counts bump. `tsc` clean; planner-wiring + pack-integrity + Troll test + fast suite green (616 files, 4678 passed).
Detail: [slice-836.md](docs/changelog/slice-836.md).

**Feat (slice 835): Shadow Strength Drain — ability-score-drain accumulator (closes `drain-undead-arms` lineage)**
The Shadow's Draining Swipe drains a target's Strength (1d4/hit, dies at STR 0) — unlike the max-HP drain, an ability score feeds ~15 derivations. New **`character.abilityDrain`** per-ability accumulator (an `AbilityScoreDrained` event + reducer, restored on a Long Rest), subtracted **last** (clamped to 1) via a new optional 4th **`drain`** param on `effectiveAbilityScore`. Threaded through every **combat/derived** consumer — attack to-hit/damage/cleave, saves, checks, AC, spell DC + attack, the heavy-weapon STR gate, the armor-STR speed penalty, hit-die heal, the sheet — but **not** the build-time validators (multiclass prereq, feat eligibility), which keep the innate score. New on-hit weapon **`drainsAbility { ability, dice }`** rider (the Shadow's `shadow-draining-swipe`, 1d6+2 necrotic + STR 1d4) with the STR-0 instant-death gate (`CreatureDestroyed` when base − cumulative drain ≤ 0). The on-kill Shadow spawn stays consumer/DM-managed. The cross-cutting change is opt-in (drain undefined for every existing character) → all conformance + golden suites byte-unchanged. +1 weapon → 239→240; new event + optional Character field (migration-safe). New 6-test slice-835. `tsc` clean; pack-integrity + doc-counts + migrations + fast suite green (614 files, 4671 passed).
Detail: [slice-835.md](docs/changelog/slice-835.md).

**Feat (slice 834): Wight Life Drain — save-action max-HP drain**
The Wight's Life Drain is a CON-save action, so slice 832's weapon `drainsMaxHp` couldn't carry it. New **`SaveActionSpec.onFail.drainMaxHp`** arm: a failed save reduces the target's Hit Point maximum by the post-mitigation damage taken (the same slice-832 `life-drained` mechanism, restored on a Long Rest). The cumulative remove-then-readd logic was extracted into a shared **`planLifeDrainEvents`** helper (the rule-of-three) now driving both the weapon on-hit drain (Specter/Wraith) and the save-action arm (Wight); `attack.ts` refactored onto it byte-compatibly (slice-832 test + fuzz goldens confirm). Content: the Wight's Life Drain save-action (CON DC 13, 1d8+2 necrotic). Advances the L7 `drain-undead-extra-arms` row; split out `drain-undead-shadow` for the Shadow STR-score drain + the Wight/Shadow on-kill spawns (consumer/DM-managed). No new condition/effect kind (reuses `life-drained`) → no doc-counts bump. New 5-test slice-834. `tsc` clean; pack-integrity + fast suite green (614 files, 4667 passed).
Detail: [slice-834.md](docs/changelog/slice-834.md).

**Infra (slice 833): combat-fuzz drop-in opponent character (PvP)**
The PvP mirror of the slice-778 player drop-in: a new `FuzzBattleOptions.opponentCharacter` (`{ character, itemInstances }`) seats a second caller-supplied sheet into team B[0] verbatim (own id / class / level / gear), so dnd-web can run real-vs-real 1v1. Built next to `playerCharacterBuilt` via the same `buildFromPlayerCharacter`; the `teamB` 'pc' branch is now index-aware and still draws-and-discards B[0]'s shared `buildL1` (keeping the seed stream aligned, exactly how teamA[0] seats the player), then seats the drop-in at index 0; the item-acquire loop emits its own instances (no synthesis) and the `level > 1` loop never re-levels it. An INDEPENDENT axis from the seed, only meaningful for the 'pc' team (ignored for `vs='monster'`). The no-`opponentCharacter` path stays byte-identical — verified by the full fuzz golden + matrix tier (139 tests, incl. the 85-test `fuzz-matrix`). New 5-test slice-833 (both drop-ins seated verbatim, seed-determinism, no seed-stream perturbation, own-level, vs=monster ignore). `tsc` clean; fast suite + fuzz goldens green.
Detail: [slice-833.md](docs/changelog/slice-833.md).

**Feat (slice 832): undead Life Drain (max-HP drain) — Specter / Wraith**
Undead Life Drain ("its Hit Point maximum decreases by an amount equal to the damage taken") was inert, and the Specter / Wraith had no weapon wired at all. Advances the [L7 audit](docs/l7-completion-audit.md) `drain-undead-arms` divergence (the attack-roll max-HP-drain arm). New **`WeaponDefinition.drainsMaxHp`** flag: a hit applies a cumulative **`life-drained`** condition carrying a negative `hpMaxBonusDelta` = the post-mitigation damage taken (the existing ConditionApplied reducer lowers `hp.maxBonus` — no new reducer; remove-then-readd keeps one cumulative entry since the reducer dedupes by id and the in-scope drainers strike once per turn). Restored on a Long Rest: `planLongRest` emits a `ConditionRemoved` (reversing the delta) for `endsOn`-longRest conditions carrying a max-HP delta, before `LongRestEnded` resets HP — precise so exhaustion / rage are untouched (the 2024 default, per the Mummy/Death-Dog "doesn't return" exceptions). Content: the previously-unmodeled **Specter** (2d6 necrotic) + **Wraith** (4d8+3 necrotic) Life Drain weapons + actions (exact dice baked via `noAbilityModifierDamage` so the drain matches the SRD line). Split out `drain-undead-extra-arms` for the Wight (save-action drain + zombie spawn) and Shadow (STR-score drain + spawn). New condition → 167 (152 rider); +2 weapons → 239; bumped the conditions + items citations. New 4-test slice-832 (drain on hit, accumulation, Long-Rest restoration). `tsc` clean; pack-integrity + doc-counts green; fast suite green (613 files, 4662 passed).
Detail: [slice-832.md](docs/changelog/slice-832.md).

**Feat (slice 831): monster Parry reaction — closes `monster-parry-reaction`**
Several martial NPCs print a Parry reaction (hit by a melee attack → +N AC vs that attack, possibly a miss) that the engine couldn't resolve — there was no generic monster reaction. Structurally `planShield` without the slot. New **`GrantParry { acBonus }`** effect (folded by `buildEffectStack` into `parryBonus()`) + **`engine.plan.parry`** (reads the bonus off the effect stack, consumes the reaction, emits a pure-notification `ParryUsed` with `preventedHit = triggeringAttackTotal < originalAC + acBonus`; the consumer drops the damage chain on a miss, same contract as Shield's `ShieldCast`) + a **`parry` reaction-affordance descriptor** (`availableReactions` / `reactionsForTrigger`, melee-only via `event.attackKind`, offered only when +N could flip the hit, monster-gated on `statblockId`). SRD-verified content: **Bandit Captain / Knight / Warrior Veteran / Noble +2, Gladiator +3** (verified the "the bandit" Parry sits on the CR 2 Captain statblock, the "## Warriors" Parry on the CR 3 Veteran; Erinyes +4 / Marilith +5 are out of scope at CR 12 / 16). See-attacker + wielding-a-weapon stay consumer-side. New effect kind → EFFECT_KINDS 69 → 70 (bumped the eight pinned citations). New 7-test slice-831. `tsc` clean; planner-wiring + doc-counts green; fast suite green (612 files, 4658 passed).
Detail: [slice-831.md](docs/changelog/slice-831.md).

**Content (slice 830): Djinni Create Whirlwind — caught condition + catch save-action (closes `monster-whirlwind-actions`)**
The last arm of the [L7 audit](docs/l7-completion-audit.md) `monster-whirlwind-actions` quirk — and the whole `monster-onhit-rider-pass` lineage. The Djinni Create Whirlwind is a Concentration-sustained moving conjured cylinder (positional → consumer-owned), but its per-creature *caught* effect decomposes into existing machinery with no new engine code: a new **`djinni-whirlwind-caught`** condition carrying Restrained's effects + `recurringDamage` {6d6 thunder, turnStart} (slice 825) + `recurringSave` {STR fixedDC 17, turnEnd, removeCondition}, applied by a **`create-whirlwind`** catch save-action (STR DC 17, no immediate damage, no size clause). The conjure point / 20-ft cylinder / 20-ft-per-turn move / area membership / "moves with the whirlwind" forced movement / the djinni's Concentration all stay consumer/DM-owned (the engine doesn't model non-spell monster Concentration). Pattern-check fix: the pack-integrity reachability walker now reads the save-action `onFail.applyConditionIds` array (plural) — this condition is the first reachable only that way. New condition → 166 (151 rider); bumped the three doc counts + the wired-conditions snapshot. New 6-test slice-830 (condition arms + save-action spec + catch fail/success + 6d6 thunder tick + end-of-turn save-to-end). `tsc` clean; pack-integrity + doc-counts green; fast suite green (611 files, 4651 passed).
Detail: [slice-830.md](docs/changelog/slice-830.md).

**Feat (slice 829): Air Elemental Whirlwind — save-action Recharge + forced push**
Extends the slice-828 `saveActions` mechanism with the two arms the Constrict family didn't exercise, closing the Air Elemental arm of the [L7 audit](docs/l7-completion-audit.md) `monster-whirlwind-actions` quirk. New **`SaveActionSpec.recharge` (`{ rechargeMin }`)** — the same Recharge economy as `breathWeapon`: `planSaveAction` marks a recharge-gated action expended on use (re-fire throws while expended), and the new `planSaveActionRechargeAtTurnStart` rolls a d6 at turn-start (wired into the three turn-start sites) and clears it on a roll ≥ `rechargeMin`; state on the new per-id `Character.expendedSaveActionIds`. New **`onFail.pushFeet`** emits a position-less `CreaturePushed` on a failed save (consumer applies the displacement). New `SaveActionExpended` / `SaveActionRecharged` lifecycle events (+ reducers / apply cases / transcript formatters), mirroring the breath-weapon pair. Content: the Air Elemental Whirlwind (Recharge 4–6, STR DC 13, ≤Medium, 4d10+2 thunder + 20-ft push + Prone, `halfDamageOnSuccess`). No new condition/effect kind (reuses prone/thunder) → no doc-counts bump; the migrations test confirms the new field's back-compat. New 5-test slice-829 (spec + fail payload + half-on-success + expended gate + end-to-end turn-start recharge). `tsc` clean; fast suite green (610 files, 4645 passed).
Detail: [slice-829.md](docs/changelog/slice-829.md).

**Feat (slice 828): monster save-action mechanism (Constrict) — closes `monster-onhit-rider-pass`**
The last open shape of the [L7 audit](docs/l7-completion-audit.md) `monster-onhit-rider-pass` quirk: the **save-action** (an auto-hit, *no-attack-roll* action resolved by a saving throw, where a failed save deals damage + applies condition(s)). The on-hit-rider machinery couldn't express it (no hit to hang on). New statblock **`saveActions: []`** field (`SaveActionSpec`: save ability/DC, reach, `maxTargetSize` gate, `onFail.damage[]` + `onFail.applyConditionIds[]`, `halfDamageOnSuccess`) + **`engine.plan.saveAction`** — the save-or-effect sibling of `breathWeapon` (single-target, no recharge, condition-primary; nothing on a success). On a fail it runs the standard mitigation/fatal-intercept/concentration pipeline (damage sourced to the monster) and stamps the monster as each condition's source (Grappled grappler resolves) + autoExpiry. Action economy is consumer-owned (Constrict bundles into Multiattack for some, standalone for others; the multiattack schema can't say "uses Constrict"). Shipped on the in-scope **Constrict** family, each SRD-verified: **Behir** (STR DC 18, ≤Large, 5d8+6, Grappled+Restrained), **Couatl** (DC 15, ≤Medium, 1d6+5, +Restrained), **Salamander** (DC 15, ≤Large, 2d6+4 + 2d6 fire, +Restrained), **Constrictor Snake** (DC 12, ≤Medium, 3d4, Grappled only). Audit corrections: **Marilith is CR 16** (out of scope) and there is no Giant Constrictor in the pack. The two whirlwind shapes (Air Elemental recharge+push+half; Djinni Concentration cylinder) are genuinely different — split out as `monster-whirlwind-actions`. New pack-integrity guard (save-action conditions resolve) + 6-test slice-828. `tsc` clean; no new effect kind/condition (reuses grappled/restrained) so no doc-counts bump; fast suite green (609 files, 4640 passed).
Detail: [slice-828.md](docs/changelog/slice-828.md).

**Infra (slice 827): CHANGELOG size discipline — evict Unreleased pointers 778-802**
The live CHANGELOG had grown to 56.9 KB, ~3 KB from the 60 KB `doc-size` ceiling, with no release cut since `0.11.0-alpha.0` (the growth is the long un-tagged Unreleased run). Applying the slice-812 eviction pattern to *un-tagged* pointers: the oldest half of the Unreleased pointers (slices 778-802) moved verbatim to [docs/changelog/archive-slices-778-802.md](docs/changelog/archive-slices-778-802.md) with a one-line reference left inline; the newer half (803-826) stays. Live file 56.9 KB → 32.5 KB; archive ~25 KB (single-Read-fit), indexed in the changelog README. Doc/infra only — no engine, content, or test change; per-slice detail files untouched.
Detail: [slice-827.md](docs/changelog/slice-827.md).

**Content (slice 826): monster on-hit rider pass (batch 4) — more grapple / prone**
Finishes the in-scope size-gated grapple/prone weapon riders that fit existing machinery (`applyConditionId` + the `target.creatureSize` / charge facts, the batch-1 shape): **6 natural weapons / 6 monsters** — Glabrezu Pincer (Grappled ≤M), Roc Talons (Grappled+Restrained ≤H), Grick Tentacles (Grappled ≤M), Barbed Devil Claws (Grappled ≤L), Chimera Ram (Prone ≤M), Mammoth Gore (charge-gated Prone ≤H). Each SRD-verified against `monsters-A-Z.md`/`animals.md`; Treant Slam + Hydra Bite spot-checked as correctly plain (no rider). Pure content. **Still open** — only the genuinely-different *save-action* shape (the Constrict actions + Djinni whirlwind: a `StrengthSavingThrow` action, no attack roll), which needs a new monster save-action mechanism, a separate engine slice. New 6-test slice-826 (per-weapon rider shape + end-to-end size gates + grappler-source + charge gate). `tsc` clean; lint/integrity/coverage green; fast suite green (608 files, 4633 passed).
Detail: [slice-826.md](docs/changelog/slice-826.md).

**Feat (slice 825): Bearded Devil infernal wound — recurring-damage condition mechanism**
Advances the [L7 audit](docs/l7-completion-audit.md) `monster-onhit-rider-pass` quirk (batch 3) with the hardest rider remainder. The engine had `recurringSave` (a per-turn save the consumer ticks) but no per-turn *damage* — the Bearded Devil's infernal wound is automatic HP loss, no save. New **`recurringDamage` condition field** ({dice, damageType, trigger}, the no-save analogue of `recurringSave`) + **`engine.plan.tickRecurringDamage`** (its sibling: rolls the per-tick dice and runs the same mitigation/fatal-intercept/concentration-on-damage pipeline as a recurring spell tick, sourced to the condition's `sourceCharacterId`; consumer-orchestrated). Content: a new `infernal-wound` condition (`recurringDamage {1d10 necrotic}` + `autoExpiry {10 rounds, bearer-keyed}` = closes after 1 minute), applied by the Infernal Glaive's onHit CON-12 save rider. RAW the loss is untyped HP loss → modeled as necrotic (closest typed approximation); the heal-closes + Medicine-stanch arms stay consumer/DM-managed (no heal→remove-condition hook). New 4-test slice-825 (wound applies on a failed save sourced by the devil; the tick drains 1d10 necrotic + lowers HP; ticking a wound-less creature throws). The new condition bumped the doc-cited count (164→165 / 150 rider). `tsc` clean; exports/coverage/doc-counts green; fast suite green (607 files, 4627 passed).
Detail: [slice-825.md](docs/changelog/slice-825.md).

**Fix (slice 824): Young Red Dragon Rend +1d6 Fire — dragon-rend rider close + RAW correction**
Closes the [L7 audit](docs/l7-completion-audit.md) `dragon-rend-no-elemental-rider` divergence — and corrects its too-broad "Dragon Rend weapons" (plural) framing. SRD-verifying every in-pack dragon's Rend: in 2024 the **chromatic** dragons carry the Rend +1dX elemental rider at *all* ages (already wired), but **metallic** dragons gain it only at **Adult+** — so the in-scope metallic wyrmling/young Rends correctly have **no** rider (wiring one would be edition drift). The lone genuine miss was the **Young Red Dragon** Rend (a chromatic young) lacking its +1d6 Fire; added. Pure content. New 2-test slice-824 — a durable invariant pinning all 18 in-pack dragon Rends to their exact RAW rider state (chromatic → element, metallic young/wyrmling → none) + the Young Red end-to-end Fire damage; the showcase golden updated (its Young Red Dragon's Rend now deals Fire, with the downstream RNG cascade). `tsc` clean; fast suite green.
Detail: [slice-824.md](docs/changelog/slice-824.md).

**Content (slice 823): monster on-hit rider pass (batch 2) — lycanthropy + save/condition riders**
Advances the [L7 audit](docs/l7-completion-audit.md) `monster-onhit-rider-pass` quirk with the on-hit *save/condition* riders that fit existing machinery (`onHit.save.conditionOnFail` + `applyConditionId`). **Were-creature lycanthropy:** a new inert `lycanthropy-cursed` marker condition applied by the Werebear/Wereboar/Wererat/Weretiger/Werewolf bites — RAW "Hit: damage. If the target is a Humanoid, CON save DC N (14/12/11/13/12). Failure: cursed" — wired as a Humanoid-gated `onHit.save{conditionOnFail}` (the "0 HP → becomes a Were-creature, GM-controlled" transform stays consumer territory, so the marker has `effects: []`; sourced by the attacker). **Unconditional riders on existing conditions:** Cloud Giant Thundercloud → Incapacitated, Oni Nightmare Ray → Frightened. Each SRD-verified against `monsters-A-Z.md`; lycanthropy is RAW-current in 2024 (not edition drift). Pure content — no engine change. The new condition bumped the doc-cited condition count (163→164 / 148→149 rider) in 3 docs. New 6-test slice-823 (marker + 7 rider shapes; curse applies to a Humanoid on a failed CON save sourced by the werewolf, but no save/curse vs an Undead; Thundercloud incapacitates, Nightmare Ray frightens). `tsc` clean; lint/integrity/coverage/doc-counts green; fast suite green.
Detail: [slice-823.md](docs/changelog/slice-823.md).

**Content (slice 822): monster on-hit rider pass (batch 1) — size-gated grapple / prone**
Advances the [L7 audit](docs/l7-completion-audit.md) `monster-onhit-rider-pass` quirk: the multiattack sweep (789-792) shipped base + secondary damage but deferred the gated on-hit riders (the natural weapons' descriptions literally said "Deferred (rider pass)"). Wired the batch using **existing conditions** the `boar-gore`/`wolf-bite` way (`applyConditionId` gated on a `target.creatureSize` predicate; the grapple stamps the attacker as grappler so the Grappled-vs-non-grappler arm resolves) — **10 natural weapons across 8 in-scope monsters**: Aboleth Tentacle, Chuul Pincer, Chain Devil Chain (+Restrained), Griffon Rend, Otyugh Tentacle, Roper Tentacle (+Poisoned, no gate), Tyrannosaurus Bite (+Restrained) & Tail (Prone), Stone Giant Boulder (Prone), Triceratops Gore (charge-gated, +2d8 + Prone). Each SRD-verified against `monsters-A-Z.md`/`animals.md` — verifying every line caught that a `+14 … Grappled DC 19` line was the **Purple Worm** (CR 15, out of scope), not the T-Rex. Player-facing descriptions updated to the now-accurate effect; escape DCs stay consumer-managed (flat `grappled`). Pure content — no engine change. Still open: the save-based Constrict actions, new-condition riders (infernal-wound, lycanthropy, frightened-on-save), and the Thundercloud/Chimera/Djinni save actions. New 8-test slice-822 (per-weapon rider shape + end-to-end size gates + grappler-source + charge gate). `tsc` clean; lint/integrity/coverage green; fast suite green (604 files, 4615 passed).
Detail: [slice-822.md](docs/changelog/slice-822.md).

**Feat (slice 821): NPC reaction discovery — monster Protective Magic in the affordance layer**
Closes the `npc-reaction-discovery` follow-up (slice 820). Slice 819 wired the Mage/Archmage *Protective Magic* (Counterspell/Shield) cast + metering, but `engine.query.availableReactions` / `reactionsForTrigger` (slices 763-767) enumerate reactions from class features / prepared spells — the Shield + Counterspell registry descriptors' `owns` checked `preparedSpells` and `correlate` required an arcane class + a 3rd-level slot, which a monster lacks — so a consumer couldn't *discover* the Mage's reactions. Extended both descriptors with a monster path: `owns` now also accepts a granted per-day pool with budget (`perDayFreeCastAvailable`, factored out of `resolvePerDayFreeCast` through a shared `resolvePerDayBudget`, gated on `statblockId` to skip the player path's effect-stack build); `correlate` branches to a `useFreeCast` intent (meters via the pool, slice 819) with `castingClassId: ''` (flat statblock DC via the `SetSpellcastingProfile`), reusing the class-independent "+5 flips the hit" / "leveled spell" structural filters. Players unaffected; the 22 existing reaction tests stay green. New 5-test slice-821 (discovery surfaces granted Counterspell/Shield; correlated `useFreeCast` intents are planner-accepted — dispatch emits `PerDayCastUsed`, no caster slot; an exhausted pool hides both; a plain fighter is offered neither). `tsc` clean; coverage/exports snapshots unchanged; fast suite green (603 files, 4607 passed). With this the `npc-caster-*` family is fully closed.
Detail: [slice-821.md](docs/changelog/slice-821.md).

**Feat (slice 820): Dryad Tree Stride — the last bonus-action-group item (and the only non-spell one)**
Fully closes the [L7 audit](docs/l7-completion-audit.md) `npc-caster-bonus-action-groups` quirk (slices 815-820). The Dryad's RAW Bonus Action *Tree Stride* teleports tree-to-tree (≤ 60 ft); the trees are terrain the engine doesn't model (positions are tracked, terrain is consumer-managed), so the tree-adjacency is a consumer seam — the engine owns the turn/economy/range/occupancy. Mirrors `planCloudsJaunt`: a new zero-arg `GrantTreeStride` marker effect (parallel to `GrantMagicResistance` — union + zod + `EFFECT_KINDS` + a `hasTreeStride()` builder flag, gating the ability data-drivenly vs hardcoding the statblock id) + a new `planTreeStride` planner (checks the marker, active turn, Bonus Action, ≤ 60 ft Chebyshev, unoccupied; emits `ActionEconomyConsumed(bonusAction)` + `CombatantMoved` with `feetTraveled: 0`; no resource — at-will given trees). Wired as `engine.plan.treeStride`, allowlisted in the planner-wiring audit. Content: the Dryad gains the marker. The new `EFFECT_KINDS` entry bumped the effect-kind count (68→69 / 67→68), so six front-door doc citations were updated. New 6-test slice-820 (teleport without movement drain; range/occupancy/bonus-action/not-a-Tree-Strider rejections). Follow-up tracked: `npc-reaction-discovery` (surfacing monster reaction groups in `availableReactions`). `tsc` clean; coverage/exports snapshots unchanged; fast suite green (602 files, 4602 passed).
Detail: [slice-820.md](docs/changelog/slice-820.md).

**Feat (slice 819): Protective Magic (3/Day) — monster Counterspell/Shield as a per-day-pooled reaction**
Advances the [L7 audit](docs/l7-completion-audit.md) `npc-caster-bonus-action-groups` quirk — its last spell item. The Mage/Archmage Reaction *Protective Magic (3/Day)* casts Counterspell or Shield, but both are dedicated slot-consuming planners (off the generic `castSpell` path, like Misty Step) sharing one 3/Day budget. With three such dedicated planners now needing slot-less NPC metering, the duplicated logic was extracted into a shared **`resolvePerDayFreeCast`** helper (resolves the `perLongRest` grant, checks the budget — pooled via slice-818 `perDayPoolId`, else per-spell — and returns the `PerDayCastUsed` to emit instead of `SpellSlotConsumed`); **Misty Step (817) was refactored onto it**, and `planCounterspell`/`planShield` use it under a new `useFreeCast` intent flag (the caster's slot becomes a per-day pool draw; the countered caster's slot loss + the CON save / +5 AC are untouched). Content: Mage + Archmage grant Counterspell + Shield (`perDayPoolId: 'protective-magic'`, 3/Day, INT). The cast+metering seam ships here; surfacing it in the reaction-discovery affordance layer is a separate query-layer concern. New 5-test slice-819 (each cast as a Reaction = reaction economy + PerDayCastUsed no caster slot; the shared-budget proof — three Counterspells block a never-cast Shield; long-rest refresh); player slot Shield/Counterspell + Misty Step stay green. `tsc` clean; coverage/exports snapshots unchanged; fast suite green (601 files, 4596 passed).
Detail: [slice-819.md](docs/changelog/slice-819.md).

**Feat (slice 818): Priest Divine Aid (3/Day) — shared per-day pool + cast-time override**
Advances the [L7 audit](docs/l7-completion-audit.md) `npc-caster-bonus-action-groups` quirk (its hardest item). The Priest's RAW Bonus Action *Divine Aid (3/Day)* casts one of Bless / Dispel Magic / Healing Word / Lesser Restoration — 3 uses **total**, all as a Bonus Action — which the per-spell slice-794 model couldn't express. Two additive `GrantSpell` fields: **`perDayPoolId`** shares one N/Day budget across grants tagged with the same id (the cast path sums each member's `perDayCastsUsed` counter vs `usesPerLongRest`; each cast still increments its own spell's counter so `PerDayCastUsed.spellId` stays accurate; long rest clears them). It needs **no resource seeding** — chosen over a `freeCastResourceId` pool because the Priest is the first monster with a resource and no consumer seeds monster resources (the per-day counter is base state). **`castAsBonusAction`** forces `castingTimeKind = 'bonusAction'` for the matched free-cast grant, so Bless + Dispel Magic (Action spells) cost the Bonus Action through Divine Aid; scoped to the grant, so a normal slot cast is unaffected. Content: the Priest gains the four pooled grants. New 4-test slice-818 (the shared-budget proof: three different spells block a fourth never-cast pool spell; long-rest refresh; Action→Bonus-Action override); slice-795's per-spell Spirit Guardians stays green. `tsc` clean; coverage/exports snapshots unchanged; fast suite green (600 files, 4591 passed).
Detail: [slice-818.md](docs/changelog/slice-818.md).

**Feat (slice 817): Misty Step per-day enabler — slot-less NPC casters**
Advances the [L7 audit](docs/l7-completion-audit.md) `npc-caster-bonus-action-groups` quirk. Misty Step has a *dedicated* planner (`planMistyStep` — it needs a destination + does the teleport, so it's off the generic `castSpell` path); that planner always expended a spell *slot* and recognized only known/prepared spells, so a slot-less Mage/Archmage (whose statblock prints "Misty Step (3/Day)") couldn't cast it. Added a `useFreeCast` branch mirroring castSpell's slice-794 machinery: a granted Misty Step (effect-stack `GrantSpell`) satisfies the "knows the spell" check, and a `perLongRest` ("N/Day") grant meters `perDayCastsUsed` + emits `PerDayCastUsed` instead of `SpellSlotConsumed`. The effect stack is built lazily so the player slot path is byte-identical (`MistyStepIntent.useFreeCast?` flows through the existing wiring; no API change). Content: `misty-step` granted 3/Day to the Mage + Archmage (RAW `monsters-A-Z.md`). New 4-test slice-817 (per-day cast = teleport + PerDayCastUsed no slot + metered; 3/Day budget enforced; granted-spell recognition; no-grant rejection); the existing `plan-misty-step` sorcerer slot test stays green. `tsc` clean; coverage/exports snapshots unchanged; fast suite green (599 files, 4587 passed).
Detail: [slice-817.md](docs/changelog/slice-817.md).

**Fix (slice 816): Spiritual Weapon's immediate attack no longer costs the Action**
Closes the [L7 audit](docs/l7-completion-audit.md) `spiritual-weapon-immediate-attack-action-cost` divergence (surfaced by slice 815). `cast-spell.ts` charged a Bonus-Action spell *both* a Bonus Action and an Action when cast at a target — slice 603's model for a spell whose attack is a *separate* Magic action (Produce Flame "take a Magic action to hurl fire"). It was keyed on a `duration !== instantaneous` heuristic that also caught **Spiritual Weapon**, whose attack RAW is made *immediately* as part of the Bonus-Action cast (no separate Action) — so every Spiritual Weapon caster (cleric, player, monster) wrongly lost their Action. Replaced the heuristic with an explicit per-attack `requiresMagicAction` flag on `SpellAttackMechanicSchema` — authored `true` on Produce Flame + Flame Blade (genuine separate Magic action), unset on Spiritual Weapon. Now the immediate-on-cast attack costs only the Bonus Action; the separate-Magic-action hurl still costs both (slice-603 behavior preserved). New 4-test slice-816 (player-cleric Spiritual Weapon = BA only; druid Flame Blade = BA + Action; flag authored right; a durable invariant guarding the heuristic→flag switch); slice-815 #4 flipped to the fixed behavior. `tsc` clean; coverage/exports snapshots unchanged; fast suite green (598 files, 4583 passed).
Detail: [slice-816.md](docs/changelog/slice-816.md).

**Content (slice 815): NPC bonus-action spell group — Cultist Fanatic Spiritual Weapon**
Partially closes the [L7 audit](docs/l7-completion-audit.md) `npc-caster-bonus-action-groups` quirk — the one item needing zero engine work. Added *Spiritual Weapon* 2/Day to the Cultist Fanatic as `GrantSpell{perLongRest:2}`: an intrinsic Bonus-Action spell on the generic `castSpell` path, so its BA economy + per-day metering fall out of the slice-794 primitive (no engine change). Triaged the rest as engine seams: Misty Step 3/Day (Mage/Archmage) routes through the dedicated slot-consuming `planMistyStep` — needs a per-day branch (correcting the slice-814 note that it rode `castSpell`); Divine Aid 3/Day (Priest) is a shared pool + Action→BA cast-time override; Protective Magic 3/Day is a Counterspell/Shield reaction; Dryad Tree Stride is a movement ability, not a spell. Surfaced a pre-existing divergence (tracked `spiritual-weapon-immediate-attack-action-cost`): casting Spiritual Weapon at a target also spends the Action via `consumesImplicitMagicAction` (built for Produce Flame's separate hurl), though RAW the immediate attack is part of the Bonus-Action cast — affects every Spiritual Weapon caster, so an engine fix not content. New 4-test slice-815 (metering + BA-only summon + characterized implicit-action). `tsc` clean; fast suite green.
Detail: [slice-815.md](docs/changelog/slice-815.md).

**Content (slice 814): NPC-caster sweep remainder — fixed-DC profiles + the Archmage**
Extends the slice 794-795 `SetSpellcastingProfile` primitive across the rest of the pack's NPC casters, closing the [L7 audit](docs/l7-completion-audit.md) `npc-caster-sweep-remainder` quirk. Audit correction: no dragon statblock actually grants spells (Fear is the dragons' *Frightful Presence* **action**, not a `GrantSpell`), so the row's "dragons derive the wrong DC" was inaccurate — the **real active bug was the Dryad**, whose Animal Friendship + Charm Monster (WIS saves) derived **spell save DC 0** with no profile. Swept the printed `SetSpellcastingProfile` onto all eight existing at-will casters (dryad CHA 14, cloud-giant CHA 15, storm-giant WIS 18, couatl WIS 15, unicorn CHA 14, deva CHA 17, planetar CHA 20, solar CHA 25 — the latter seven cast only no-save spells today, so latent), completed the Dryad's RAW 1/Day bucket (Entangle, Pass without Trace), and authored the **Archmage**'s full Spellcasting action (INT, DC 17). Pure content — no engine change. New 21-test slice-814 (each DC pinned + derived end-to-end via `computeSpellSaveDC`, plus a durable invariant: no monster grants a save-spell without a profile). The bonus-action/reaction spell groups split out as the tracked `npc-caster-bonus-action-groups` row. `tsc` clean; fast suite green (596 files, 4575 passed); coverage/exports snapshots unchanged.
Detail: [slice-814.md](docs/changelog/slice-814.md).

**Content (slice 813): player-facing rules text for every spell (`description`)**
Spells carried no `description` at all — the structured mechanics ran, but dnd-web's tap-to-read panel (the surface slice 811 cleaned for items/conditions) rendered blank for any spell. Closes that explicitly-tracked gap: a scripted, verify-after extraction pulled clean rules prose for **all 339 spells** from `references/srd-markdown/spells.md` (the CC-BY-4.0 SRD 5.2.1 clone — matched by name, 0 misses), dropping the school/metadata lines and stripping markdown (`**`, `_…_`, `<br>`, `&emsp;`, table pipes). Additive display field only — no mechanic/planner/event/DC change, and the event log doesn't embed `description`, so no golden transcript moves; the text is 2024-faithful by construction (e.g. Sleep = WIS-save/Incapacitated, Color Spray = CON-save/Blinded, per slices 783/784). New `spell-descriptions` guard (fails on a missing or `<40`-char description); the slice-811 lint now walks spells too (0 artifacts). `tsc` clean; fast suite green.
Detail: [slice-813.md](docs/changelog/slice-813.md).

**Infra (slice 812): CHANGELOG size discipline — evict the `0.11.0-alpha.0` release narrative**
The live CHANGELOG crossed the 60 KB single-Read ceiling (60,638 B), tripping the `doc-size` audit. Per the active-cycle invariant (the live file holds only the active cycle plus the newest release until doc-size pressure evicts it — the same trigger slice 754 used on 0.10.0), the tagged `0.11.0-alpha.0` cohort (slices 749-777) moves verbatim to [docs/changelog/released-versions-0.11.0-alpha.0.md](docs/changelog/released-versions-0.11.0-alpha.0.md), with a pointer added to "Older releases." Live file 60.6 KB → ~36 KB. Doc/infra only — no engine, content, or test-behavior change; per-slice detail files are untouched.
Detail: [slice-812.md](docs/changelog/slice-812.md).

**Content (slice 811): player-facing `description` cleanup (rules text vs engine notes)**
dnd-web's tap-to-read rules feature shows content `description` verbatim to players, so it's now an end-user string — but ~247/480 fields mixed SRD rules with engineering notes (`Slice <n>` refs, `RAW (…):` wrappers, consumer/implementation commentary). Option A: `description` is now clean player-facing SRD-style text; the dev annotation moves to a new optional `engineNotes` field (added to Spell/Item/Condition/ClassFeature schemas), set to the full original so nothing is lost. Scope reality: descriptions live on items (324) + conditions (153) + 3 class features — **spells carry none** (adding spell rules text is a separate tracked gap). A scripted, dry-run-first transform extracted the rules text (keep RAW quote + flavor, drop the wrapper + `Slice` notes; reword potion-drinker "consumer" → "the creature"; 32 internal marker-conditions → empty description). New `player-facing-descriptions` lint test (fails on `/Slice \d/`, `/\bRAW\b/`, `/consumer/`) landed red then cleaned green. No mechanical change — fast suite green (594 files, 4551 passed); the engine event log doesn't embed `description`.
Detail: [slice-811.md](docs/changelog/slice-811.md).

**Feat (slice 810): multiclass ability prerequisites (`validateMulticlass`)**
Nothing enforced the 13+ ability prereq for multiclassing (an INT 8 character could carry a Wizard level). Multiclass entry is snapshot-only (no planner gate), so — like slice 793's `validateBackgroundAbilityIncrease` — the engine ships a consumer validator: new exported `validateMulticlass(character, content, options?)` returns one issue per class whose `primaryAbility` 13+ prereq is unmet (effective score). New `Class.multiclassAbilityMode` ('any'/'all', default 'all') resolves the or/and from the SRD "Primary Ability" phrasing (Fighter "Strength OR Dexterity" = any; Monk/Paladin/Ranger "X AND Y" = all). **Closes the [L7 audit](docs/l7-completion-audit.md) Area 5 divergence `multiclass-prereqs`.** New 5-test slice-810; +2 public exports; fast suite green.
Detail: [slice-810.md](docs/changelog/slice-810.md).

**Fix (slice 809): L4 feat-menu eligibility filter**
The L4 ASI/feat choice offered a static {ASI, Grappler} to everyone, ignoring Grappler's STR/DEX-13 prereq — feat `prerequisites` was free-text strings, not machine-checkable. New structured `Feat.abilityPrerequisite` ({abilities, min}, the display-text stays); `planLevelUp` drops feat-options whose `GrantFeat`'s ability prereq is unmet (effective score, "or" semantics). Authored on Grappler. The Fighting-Style-injection arm (offering Fighting Style feats to feature-bearing classes) is split out as the tracked `l4-menu-no-fighting-style-feats` quirk — a missing option, not a wrong outcome. **Closes the [L7 audit](docs/l7-completion-audit.md) Area 5 divergence `l4-feat-menu-eligibility`.** New 4-test slice-809; fast suite green.
Detail: [slice-809.md](docs/changelog/slice-809.md).

**Feat (slice 808): Grappler feat + Savage Attacker audit correction**
The Grappler feat was inert (`effects: []`); now authored to RAW — an ASI `OfferChoice` (STR/DEX +1) + Attack Advantage (`SetAdvantage` gated on a new `event.targetGrappledByAttacker` attack-planner fact); Punch-and-Grab / Fast-Wrestler arms deferred. Separately, `savage-attacker-feat-inert` is a **stale audit false-positive**: slice 467 already implemented Savage Attacker (tested) via `useSavageAttacker` + the effective-feat-list check — its empty `effects` is correct. **Closes the [L7 audit](docs/l7-completion-audit.md) Area 5 divergence `grappler-feat-inert` and reclassifies `savage-attacker-feat-inert`** (Area 5 divergences 5 → 3). New 3-test slice-808; coverage snapshot +`general:grappler`; fast suite green.
Detail: [slice-808.md](docs/changelog/slice-808.md).

**Fix (slice 807): Charmed — can't harm the charmer (spells) + social advantage**
The `charmed` condition carried 0 effects: the engine blocked weapon attacks on the charmer but a Charmed caster could still fire a harmful spell at them, and the charmer's social-check advantage was unmodeled. `planCastSpell` now blocks a harmful spell (attack/save mechanic) that explicitly targets the charmer (AoE membership exempt — an area the charmer stands in isn't "targeting" them); beneficial spells (Mage Armor, heals) on the charmer are fine. The social-advantage arm ships as a consumer fact `ComputeAbilityCheckInput.socialCheckTargetId` (Advantage on a Persuasion/Deception/Intimidation/Performance check vs a creature charmed by the checker). Both key on `sourceCharacterId`, so the condition stays effect-less. **Closes the [L7 audit](docs/l7-completion-audit.md) Area 4 divergence `charmed-harmful-target-arm` — Area 4 is now divergence-free.** New 3-test slice-807; fast suite green.
Detail: [slice-807.md](docs/changelog/slice-807.md).

**Fix (slice 806): one spell slot per turn**
The audit row described the 2014 rule ("Bonus-Action spell → Action must be a cantrip"), which 2024 removed; the real SRD 5.2.1 rule (spells.md) is "you can expend only one spell slot to cast a spell" per turn — so a slot spell + a cantrip is fine, but two slots (e.g. Bonus Action Spiritual Weapon + Action Fireball) isn't. New `TurnUsage.spellSlotExpendedThisTurn` flag, set when a `SpellSlotConsumed`/`PactSlotConsumed` lands in an encounter (reset at TurnStarted); `planCastSpell` blocks a second slot-expending cast that turn (cantrips/rituals/free casts exempt; pact slots count). Implementing the row's literal 2014 wording would have introduced edition drift — the audit row is corrected. **Closes the [L7 audit](docs/l7-completion-audit.md) Area 4 divergence `bonus-action-spell-restriction`.** New 4-test slice-806; fast suite green.
Detail: [slice-806.md](docs/changelog/slice-806.md).

**Fix (slice 805): synthetic-Unconscious arms on a 0-HP drop**
A creature at 0 HP got the within-5-ft auto-crit (keyed on `hp <= 0`) but not the Unconscious condition's "attackers have Advantage" or "auto-fail STR/DEX saves" arms — those live on the condition's effects, which the HP-drop path never applies, so a downed creature was attacked without Advantage (and the dragon's killing blow in the showcase transcript actually *missed*). Made both arms synthetic on `hp.current <= 0` too: `targetGrantsAdvantage` in attack.ts and `hasAutoFail` (STR/DEX) in save.ts. Keeps `hp <= 0` the single source of truth (no dual-tracked condition to desync on heal); composes with the explicit `unconscious` condition (Sleep). Prone / drop-items stay deferred. **Closes the [L7 audit](docs/l7-completion-audit.md) Area 4 divergence `drop-to-0-no-unconscious-arms`.** New 4-test slice-805; showcase golden updated (the downed-PC attack now lands with Advantage → auto-crit); fast suite green.
Detail: [slice-805.md](docs/changelog/slice-805.md).

**Fix (slice 804): Armor Training penalties (untrained armor)**
The class `armorProficiencies` arrays were never read, so RAW Armor Training did nothing — a Wizard in Plate rolled STR/DEX normally, cast freely, and any shield gave +2 AC. New `derive/armor-training.ts` resolves training (class arrays + effect-stack `GrantProficiency('armor')`, mirroring `isWeaponProficient`) and the three RAW effects apply at their five sites: Disadvantage on STR/DEX checks (ability-check), saves (save), and weapon attacks (attack, beside the Heavy-weapon rule); can't cast (cast-spell gate); and a shield gives no AC untrained (AC gate). Monsters (armorClass override, no equipped armor) are unaffected. **Closes the [L7 audit](docs/l7-completion-audit.md) Area 6 divergence `untrained-armor-penalty`.** New 6-test slice-804; slice-798's stealth test re-based onto a Fighter to isolate its arm; fast suite green (587 files, 4524 passed).
Detail: [slice-804.md](docs/changelog/slice-804.md).

**Fix (slice 803): Grapple / Shove RAW gates**
`planGrapple`/`planShove` rolled the save but skipped the RAW preconditions — a Stunned Medium PC could grapple a Gargantuan dragon two-handed. Added three gates (reusing existing helpers): `assertActorCanAct` (both); a size gate (`SIZES` rank diff ≤ 1 — target no more than one size larger, both); and a free-hand gate (grapple only — a two-handed weapon, or main-hand + off-hand/shield, leaves no free hand). Shove keeps no free-hand requirement per RAW. **Closes the [L7 audit](docs/l7-completion-audit.md) Area 4 divergence `grapple-shove-missing-gates`.** New 6-test slice-803; fast suite green.
Detail: [slice-803.md](docs/changelog/slice-803.md).

**Earlier Unreleased slices (778-802)** — their per-slice pointers are archived in [docs/changelog/archive-slices-778-802.md](docs/changelog/archive-slices-778-802.md) to keep this file under the single-Read ceiling; full detail lives in the `slice-NNN.md` files. Still part of the active, un-tagged cycle (no release cut since `0.11.0-alpha.0`).

## Older releases

Tagged release `0.11.0-alpha.0` lives in [docs/changelog/released-versions-0.11.0-alpha.0.md](docs/changelog/released-versions-0.11.0-alpha.0.md); `0.10.0-alpha.0` lives in [docs/changelog/released-versions-0.10.0-alpha.0.md](docs/changelog/released-versions-0.10.0-alpha.0.md); `0.9.0-alpha.0` lives in [docs/changelog/released-versions-0.9.0-alpha.0.md](docs/changelog/released-versions-0.9.0-alpha.0.md); `0.8.0-alpha.0` lives in [docs/changelog/released-versions-0.8.0-alpha.0.md](docs/changelog/released-versions-0.8.0-alpha.0.md); `0.7.0-alpha.0` lives in [docs/changelog/released-versions-0.7.0-alpha.0.md](docs/changelog/released-versions-0.7.0-alpha.0.md); `0.6.0-alpha.0` lives in [docs/changelog/released-versions-0.6.0-alpha.0.md](docs/changelog/released-versions-0.6.0-alpha.0.md); `0.5.0-alpha.0` lives in [docs/changelog/released-versions-0.5.0-alpha.0.md](docs/changelog/released-versions-0.5.0-alpha.0.md); `0.4.0-alpha.0` lives in [docs/changelog/released-versions-0.4.0-alpha.0.md](docs/changelog/released-versions-0.4.0-alpha.0.md); `0.3.0-alpha.0` lives in [docs/changelog/released-versions-0.3.0-alpha.0.md](docs/changelog/released-versions-0.3.0-alpha.0.md); `0.2.0-alpha.0` lives in [docs/changelog/released-versions-0.2.0-alpha.0.md](docs/changelog/released-versions-0.2.0-alpha.0.md); `0.1.0-alpha.15` lives in [docs/changelog/released-versions-alpha-15.md](docs/changelog/released-versions-alpha-15.md); `0.1.0-alpha.14` lives in [docs/changelog/released-versions-alpha-14.md](docs/changelog/released-versions-alpha-14.md); `0.1.0-alpha.6` through `0.1.0-alpha.13` live in [docs/changelog/released-versions-alpha-6-13.md](docs/changelog/released-versions-alpha-6-13.md); `0.1.0-alpha.0` through `0.1.0-alpha.5` (the pre-rename `ttrpg-engine-dnd` package, all unpublished from npm in May 2026 on IP-cleanup grounds) live in [docs/changelog/released-versions.md](docs/changelog/released-versions.md). Per-cohort slice-detail archives are indexed in [docs/changelog/README.md](docs/changelog/README.md).
