# Starter pack coverage and gaps

This doc is the **priority queue** for contributor work and a top-level summary of what currently ships in [src/content/packs/starter-pack.json](../src/content/packs/starter-pack.json) versus what's deferred. Detailed per-category catalogs live in sibling `gaps-*.md` files; this doc keeps the priority-queue tables (Future engine slices + Deferred primitives backlog) inline so a contributor reading it cold has the actionable list directly.

## How to pick a slice (for new contributors)

1. **Read [CLAUDE.md](../CLAUDE.md) first** if you haven't — slice cadence, branch flow, pre-commit Uncle Bob audit, SRD canon, doc size discipline. Those rules apply to every slice.
2. **Skim the "Coverage at a glance" table below** for the lay of the land.
3. **Jump to "Future engine slices"** further down for the catalog of focused primitives, each with the cohort of spells / features / items it unblocks. Rows ranked roughly by payoff.
4. **Also check "Deferred primitives backlog"** further down for small, accreted RAW deviations that each have a one-primitive fix. Lower-payoff per slice but excellent for getting familiar with the codebase.
5. **Pick a row, follow [docs/slice-template.md](slice-template.md)**, commit to `dev`, surface the work.

If you're not sure which row is approachable, the smallest-scope rows in "Deferred primitives backlog" (single-feature unblockers) are good first slices. Large structural primitives (new event types, new TriggerAction variants) are larger commits and best done after one or two warm-up slices.

If you need per-category detail beyond the slim "Coverage at a glance" table below (which row of spells is still schema-only, which class features wait on which primitive, which magic items need `UseItem` variants, what RAW monster mechanics still ship as `traits: []`), drill into the per-category catalog files listed under "Per-category catalogs" below.

## Relationship to other docs

This is separate from [content-attribution.md](content-attribution.md), which is a licensing audit (what's clearly SRD-derived vs needs verification). The two docs are kept in parallel: attribution tracks "may we ship this?", this doc tracks "is it actually in here, and how completely?".

Sibling per-category catalogs (extracted in slice 249 to keep this doc under the single-Read ceiling — see [CLAUDE.md](../CLAUDE.md) "Doc size discipline"):

- [gaps-spells.md](gaps-spells.md) — per-spell wired vs schema-only catalog (L0 through L9).
- [gaps-class-features.md](gaps-class-features.md) — per-class stub-features inventory.
- [gaps-items-batches-1.1-1.10.md](gaps-items-batches-1.1-1.10.md) + [gaps-items-batches-1.11-1.20.md](gaps-items-batches-1.11-1.20.md) — per-item RAW-shape-deferred mechanical wiring.
- Monsters split across cohorts: [gaps-monsters-batches-5.9-5.11.md](gaps-monsters-batches-5.9-5.11.md) (most recent SRD closure), [gaps-monsters-batches-5.6-5.8.md](gaps-monsters-batches-5.6-5.8.md), [gaps-monsters-batches-5.1-5.5.md](gaps-monsters-batches-5.1-5.5.md), [gaps-monsters-batches-4.8-4.14.md](gaps-monsters-batches-4.8-4.14.md), [gaps-monsters-batches-4.1-4.7.md](gaps-monsters-batches-4.1-4.7.md), [gaps-monsters-batches-1.md](gaps-monsters-batches-1.md) (MM seed + earliest batches), [gaps-monsters-deferred-mechanics.md](gaps-monsters-deferred-mechanics.md) (per-RAW-trait gap catalog).

## Coverage at a glance

| Category | In pack | Rough PHB total | Notes |
|---|---|---|---|
| Classes | 12 / 12 | 12 | All scaffolded with 1-20 level tables. Most L2+ rows ship empty; see [gaps-class-features.md](gaps-class-features.md). |
| Subclasses | 12 / ~50 | ~50 | One canonical L3 subclass per class. ~13 outstanding higher-tier features documented inline below. |
| Species | 9 / ~10 | ~10 | SRD 5.2.1 complete; Aasimar is PHB-only (non-SRD). |
| Backgrounds | 19 / 16 | 16 | Full PHB 2024 list shipped (plus three legacy entries for round-trip compat). |
| Feats | 35 total | ~50+ | 12 origin / 7 general / 6 fighting style / 10 epic boon. SRD 5.2.1 complete. |
| Spells | 351 (339 SRD + 12 non-SRD) | ~340 (SRD 5.2.1) | ~147 mechanically wired (139 via `mechanicalEffects`, 11 dedicated planners). Per-level wired-vs-schema-only catalog in [gaps-spells.md](gaps-spells.md). |
| Items | 510 total | hundreds (DMG) | 46 weapons + 22 armor + 69 consumables + 37 tools + 77 gear + 259 magic items. Slices 315-316 did the magic-equipment modeling: single-base magic armor/shields (9, slice 315) re-modeled `magic`→`armor` (AC derive grants base AC + acBonus; effects project when worn + attuned), and single-base magic weapons (7, slice 316: Sun Blade, Dwarven Thrower, Dagger of Venom, Scimitar of Speed, Mace of Smiting, Thunderous Greatclub, Quarterstaff of the Acrobat) re-modeled `magic`→`weapon` (attack planner applies `attackBonus`/`damageBonus`/`onHit` riders; effects project; counts as magical). Multi-base magic equipment (Dwarven Plate, Elven Chain, Mithral, Adamantine, Frost Brand, Flame Tongue, "Weapon/Armor +1/+2/+3", etc.) stays `itemKind: magic` (deferred): no single base; needs a magic-property-applied-to-a-chosen-base model. Conditional weapon riders (vs Undead / Giants / Constructs) and charged/reaction arms also deferred. Slices 305-312 wired a magic-item buff sweep: slice 305 (Ring of Feather Falling, Gloves of Thievery + 3 potions converted `magic`→`consumable`), slice 306 (Ioun Stone of Awareness, Robe of the Archmagi fully, Belt of Dwarvenkind's Resilience arm), slice 307 (Spellguard Shield's Magic Resistance arm, Armor of Invulnerability's B/P/S resistance arm), slice 308 (new `IncreaseAbilityScore` primitive + the six ability Ioun Stones and Belt of Dwarvenkind's Toughness arm), slices 309-310 (categorization fixes: 4 SRD-Potion items + the 10 generic Spell Scroll templates reclassified `magic`→`consumable`, each guarded by a new audit check), slice 311 (passive arms of 6 staves/rods/medallion: Staff of Fire/Frost resistance, Rod of Alertness advantage, Scarab of Protection +1 AC + magic resistance, Staff of the Magi / Staff of Power spell-attack + AC + save bonuses), slice 312 (passive arms of 5 more: Robe of Eyes truesight/darkvision 120 + Perception advantage, Frost Brand fire resistance, Quarterstaff of the Acrobat acrobatics advantage, Robe of Stars / Luck Blade +1 all-saves). Per-item RAW shape catalog in [gaps-items-batches-1.1-1.10.md](gaps-items-batches-1.1-1.10.md) + [gaps-items-batches-1.11-1.20.md](gaps-items-batches-1.11-1.20.md). |
| Monsters | 253 / ~370 | ~370 (MM) | All 14 MM creature types in pack; SRD 5.2.1 monster catalog complete (235/235). Per-batch authoring history + per-RAW-trait deferred mechanics in the `gaps-monsters-*` sibling files. |
| Conditions | 116 (15 RAW + 101 rider) | 15 (RAW) | All 15 RAW conditions plus 101 mechanic-rider conditions used by the engine. Slices 301-302 added 4 buff conditions; slice 304 removed 6 dead 2014-era orphans; slice 305 added potion-of-invulnerability-active. |

## Spells

Per-spell wired-vs-schema-only catalog at L0 through L9 moved to **[gaps-spells.md](gaps-spells.md)** in slice 249.

## Class features

Per-class stub-features inventory moved to **[gaps-class-features.md](gaps-class-features.md)** in slice 249.

## Subclasses

One canonical L3 subclass ships per class. ~13 outstanding higher-tier subclass features (L6-L20) are still on the menu; the SRD 5.2.1 progression points and per-batch wiring history moved to **[gaps-class-features.md](gaps-class-features.md)** in slice 249.

## Items

Per-item RAW-shape catalog moved to **[gaps-items-batches-1.1-1.10.md](gaps-items-batches-1.1-1.10.md)** + **[gaps-items-batches-1.11-1.20.md](gaps-items-batches-1.11-1.20.md)** in slice 249.

## Monsters

Per-batch authoring history (5.x, 4.x, 1.x cohorts) and per-RAW-trait deferred-mechanics catalog moved to the **`gaps-monsters-*.md`** sibling files in slice 249. See "Per-category catalogs" under "Relationship to other docs" above for the file list.

## Species

Seven species: Human, Elf, Dwarf, Halfling, Tiefling, Dragonborn, Gnome. PHB 2024 adds Aasimar, Goliath, and the 2024-edition Orc as a playable species. None are in the pack.

## Backgrounds

The full PHB 2024 list (16) is shipped: Acolyte, Artisan, Charlatan, Criminal, Entertainer, Farmer, Folk Hero, Guard, Guide, Hermit, Merchant, Noble, Outlander, Sage, Sailor, Scribe, Soldier, Wayfarer. Plus three legacy entries kept for round-trip compatibility.

## Feats

- **Origin (12):** Savage Attacker, Alert, Magic Initiate (Cleric / Wizard / Druid variants), Tough, Skilled, Crafter, Lucky, Healer, Musician, Tavern Brawler.
- **Fighting Style (6):** Archery, Defense, Dueling, Great Weapon Fighting, Protection, Two-Weapon Fighting.
- **General (6):** Great Weapon Master, Sharpshooter, Polearm Master, War Caster, Resilient (Constitution variant), Unarmored Defense (Barbarian).
- **Epic Boon (9):** Combat Prowess, Dimensional Travel, Energy Resistance, Fortitude, Irresistible Offense, Skill, Spell Recall, the Night Spirit, Truesight.

PHB 2024 ships ~30 general feats and ~25 origin feats; this pack carries the most common. The other ~20+ general feats (Ability Score Improvement, Athlete, Charger, Crusher, Defensive Duelist, Fey Touched, Heavily Armored, Inspiring Leader, Keen Mind, Magic Initiate as a general feat, Mage Slayer, Mounted Combatant, Observant, Piercer, Poisoner, Sentinel, Shadow Touched, Shield Master, Skill Expert, Skulker, Slasher, Speedy, Tavern Brawler upgrade tier, Telekinetic, Telepathic, Weapon Master) and the remaining origin feats are deferred.

## Conditions

All 15 RAW conditions ship (Blinded, Charmed, Deafened, Exhaustion, Frightened, Grappled, Incapacitated, Invisible, Paralyzed, Petrified, Poisoned, Prone, Restrained, Stunned, Unconscious). Twenty engine-mechanic conditions also ship to back rider effects (e.g. `sapped`, `vexed-by`, `slowed-10ft` for weapon masteries; `blessed`, `mage-armored`, `guided`, `concentrating`; multi-effect buff conditions `foresight-active`, `mind-blanked-active`, `invulnerable-active`; type-conditional wards `protection-from-evil-and-good-active`, `magic-circle-active`, `holy-aura-active`; plus the slice-106 utility buffs `blade-warded-active`, `resisted`, `longstrider-active`, `darkvision-active`). Complete.


## Future engine slices (what unblocks the deferred spells)

Each entry below is one engine primitive: a focused engine slice that, once landed, retro-wires a cohort of spells currently shipping schema-only. Ranked roughly by spell-count payoff. When a slice lands, mark it `✓ shipped (slice N)` and walk the affected spells from schema-only to wired in the per-level breakdowns above.

| Primitive | Spells unblocked | Notes |
|---|---|---|
| **Alter Self** (spell wiring) | 1 (alter-self) | Slice 287 refrained the original "Transformation handler" row: RAW for both alter-self and gaseous-form is buff-condition shaped, not statblock-swap shaped, so no wildShape/polymorph-style planner is needed. Gaseous Form shipped slice 287 via the existing slice-73 buff mechanic. Alter Self stays deferred — all three of its arms need primitives the engine doesn't have: Aquatic Adaptation needs the non-walk speed derive + a "matchWalkSpeed" `ModifySpeed` op (same blocker as Cloak of Arachnida climb speed); Natural Weapons needs unarmed-strike attack replacement (1d6 of caster-chosen type + use spellcasting mod instead of STR + counts as magical); Change Appearance is pure narrative (no engine work). Re-purpose this row as the per-arm tracker once the prerequisite primitives land. |

Slice 270 archived the ~30 shipped engine-slice primitives from this table to [gaps-engine-slices-shipped.md](gaps-engine-slices-shipped.md). The live table above carries only the unshipped rows; reach for the archive when you need the historical view of what each shipped primitive landed.

## Deferred primitives backlog (small, accreted over recent slices)

These are smaller deferrals from individual content slices: missing primitives, predicate facts, or planner shapes that block a specific feature or RAW arm. Each row names the slice where the deferral was first noted plus what specifically would unblock it. Bundle nearby entries when shipping the unblocking primitive (e.g. one slice for the three "ModifySpeed" extensions).

| Deferred feature | First noted | Unblocker (smallest primitive that closes it) |
|---|---|---|
| ~~Additive ability-score increase (+N to a max) — 6 ability Ioun Stones, Belt of Dwarvenkind Toughness~~ | ~~slice 306~~ | ~~Closed by slice 308: new `IncreaseAbilityScore { ability, amount, max }` effect kind, distinct from `OverrideAbilityScore` (which *sets*/floors). `effectiveAbilityScore` applies floor-then-increase and the increase only raises (never clamps a higher floor down to its cap). Wired the six ability Ioun Stones (+2 to max 20) and Belt of Dwarvenkind's Toughness arm.~~ **Still open (separate shape):** the 4 Manual/Tome items permanently raise the score AND its max on a one-time read — that needs a one-time-permanent application path, not a passive worn projection. |
| Hunter's Mark — Advantage on WIS (Perception or Survival) checks to find the marked target | slice 222 | New effect kind `GrantAdvantageVsBearersOfMyCondition { conditionId, skills }`, read by the caster's skill-check derive |
| Hunter's Mark — bonus-action remark on target death | slice 222 | Dedicated `planRemarkHuntersMark` planner (consumes bonus action, moves `hunters-mark-active` from old target to new) |
| Hunter's Mark — upcast-driven duration extension (1 hr → 8 hr at L3-4, 24 hr at L5+) | slice 222 | Concentration system carries round-based expiry today; add hour-tier duration semantics |
| ~~Ranger L17 Precise Hunter~~ | ~~slice 217 (rolled-forward)~~ | ~~Closed by slice 231: new `GrantAdvantageVsBearersOfMyCondition` Effect kind threaded into the planAttack advantage resolution. Wired as a single content edit on top of the slice-222 hunters-mark-active condition.~~ |
| Monk L10 Heightened Focus | slice 217 (rolled-forward) | `planFlurryOfBlows`, `planPatientDefense`, `planStepOfTheWind` (Monk L2 bonus-action planners — all currently content stubs) |
| Cleric L20 Greater Divine Intervention — 2d4-LR cooldown when Wish is the chosen spell | slice 221 | New `ResourceCooldownExtended` primitive that the rest reducer can honor by skipping `divine-intervention` recharge until the cooldown count reaches zero |
| Sorcerer L7 Sorcery Incarnate — doubled-metamagic-per-spell arm | slice 201 (rolled-forward) | Once-per-spell metamagic enforcement in the metamagic planner |
| ~~Troll + Troll Limb — Regeneration trait~~ | ~~slice 226~~ | ~~Closed by slice 232: new `Regeneration { perTurn, suppressedBy }` Effect kind + `damageTypesTakenThisTurn` Character field + planRegenerationAtTurnStart helper. Troll wires 15 HP/turn (suppressed Acid/Fire); Troll Limb 5 HP/turn (same). Retro-wirable for Werewolf, Hag, Vampire when those monster traits are authored.~~ |
| ~~Troll — Loathsome Limbs spawn~~ | ~~slice 226~~ | ~~Closed by slice 233: new SpawnCreature TriggerAction + per-type damage facts on DamageApplied + attack planner now dispatches on DamageApplied. Three RAW deviations documented in slice (Bloodied gate, turn-end timing, 4/Day cap) — opportunities for follow-up tightening if a bearer.bloodied predicate, a TurnEnded trigger event, and a per-encounter resource shape land.~~ |
| ~~Cloak of Arachnida — climb speed equal to walk speed~~ | ~~slice 227~~ | ~~Closed by slice 290: new `ModifySpeed { op: 'matchWalkSpeed', value: 0 }` op + slice-288 resolver handles the new branch. The op recurses once into `getEffectiveSpeedForMode(input, 'walk')` to find the effective walk speed (honoring Fast Movement, Unarmored Movement, Haste's ×2, etc.), then treats it as a `set` for the non-walk mode. Cloak of Arachnida re-wired (was a pre-290 `op: 'set', value: 30` approximation); Slippers of Spider Climbing and the `spider-climbing-active` condition (Spider Climb spell + Potion of Climbing) also re-wired. RAW deviations remaining on Cloak of Arachnida: Athletics-climb advantage (would gate on slice-274's `athleticsSubAction='climb'`); "communicate with spiders" is narrative; the Web spell-cast-as-item-action arm still deferred (needs the slice-241 CastSpell UseAction variant on Cloak).~~ |
| ~~Eyes of the Eagle — Advantage restricted to sight-based Perception~~ | ~~slice 227~~ | ~~Closed by slice 263: new `sense?: 'sight' \| 'hearing' \| 'smell' \| 'touch' \| 'taste'` field on `ComputeAbilityCheckInput`; `computeAbilityCheck` populates `event.sense` fact and threads to `advantageFor` (uses the slice-258 SetAdvantage.condition plumbing). Eyes of the Eagle re-wired with predicated SetAdvantage gated on `eq path:'event.sense' value:'sight'`. Pattern-check surfaced 2 sibling broader-than-RAW SetAdvantage wires (Cloak of the Bat Stealth, Gloves of Swimming and Climbing Athletics); see deferred rows below.~~ |
| ~~Cloak of the Bat — Advantage on Stealth restricted to dim light or darkness~~ | ~~slice 263~~ | ~~Closed by slice 279: new `lightLevel?: 'bright' \| 'dim' \| 'darkness'` field on `ComputeAbilityCheckInput`; `computeAbilityCheck` populates `bearer.lightLevel` fact alongside existing slice 263/274/276 fact slots. Cloak of the Bat's Stealth SetAdvantage gates on `any(eq value:'dim', eq value:'darkness')`. Same opt-in semantic as Eyes of the Eagle and Gloves of Swimming (undefined produces no advantage). The fly-speed and Polymorph-to-Bat arms (deferred rows below) share the same fact but additionally need the slice-242 Toggle UseAction wire and a Polymorph cross-reference primitive respectively.~~ |
| ~~Gloves of Swimming and Climbing — Advantage on Athletics restricted to climb/swim sub-actions~~ | ~~slice 263~~ | ~~Closed by slice 274: new `athleticsSubAction?: 'climb' \| 'swim' \| 'jump' \| 'grapple' \| 'shove'` field on `ComputeAbilityCheckInput` (mirror of slice 263's `sense?` field); `computeAbilityCheck` populates `event.athleticsSubAction` fact alongside `event.sense`. Gloves of Swimming and Climbing wired with `SetAdvantage` gated on `any(eq path:event.athleticsSubAction value:climb, eq path:event.athleticsSubAction value:swim)`. The five-value enum covers 2024 PHB-named Athletics sub-actions. Same undefined-means-no-match semantics as slice 263's sense fact (consumer who knows the narrative context populates).~~ |
| ~~Cloak of the Bat — Fly Speed 40 active in Dim Light or Darkness~~ | ~~slice 227~~ | ~~Closed by slice 289 (composes slice 240 + 279 + 288): 1 charge / dawn recharge + `onUse: [{ kind: 'ApplyCondition', conditionId: 'cloak-of-the-bat-active' }]`. The condition carries `ModifySpeed fly set 40`, now mechanically observable via slice-288's `getEffectiveFlySpeed`. The dim-light activation gate stays consumer-managed (same shape as Pipes of Haunting's 30-ft scope; engine doesn't model scene lighting at activation time); once the buff is applied, fly speed persists for the hour regardless of where the wearer walks. The 1-hour duration is consumer-managed (mirror of slice 236's ApplyCondition doc).~~ |
| Cloak of the Bat — Polymorph-self-to-Bat in Dim Light or Darkness | slice 227 | Light-level fact landed slice 279. Still needs a `castSpell` cross-reference primitive that uses the dedicated `planPolymorph` (Polymorph is a dedicated planner, not a regular cast). Parallel deferral to slice-237's Spell Scroll of Misty Step / Wish row. |
| ~~Bracers of Defense — no-shield clause on the +2 AC~~ | ~~slice 227~~ | ~~Closed by slice 230: added `bearer.wieldingShield` predicate fact in src/derive/ac.ts (mirror of slice-116's wearingArmor fact). Bracers of Defense now wires `AddModifier ac 2` with `all(wearingArmor=false, wieldingShield=false)` condition for exact RAW.~~ |
| ~~Armor of Resistance / Ring of Resistance — chooser-at-creation damage type~~ | ~~slice 224~~ | ~~Closed by slice 295 via variant unroll (same pattern as Belt of Giant Strength variants): 10 entries per parent (one per damage type from the SRD 5.2.1 d10 table — acid, cold, fire, force, lightning, necrotic, poison, psychic, radiant, thunder). Each variant id is `<parent>-<type>`, named `<Parent> (<Type>)`. Effects array carries a single `GrantResistance { damageType }` for the variant's type. Armor variants stay attunement-required (RAW); Ring variants stay no-attunement (slice 184 SRD-drift fix). The single empty parent entries were removed (no consumers reference them; verified via grep across src/ tests/ docs/). Potion of Resistance still deferred (separate row) — adds the additional dimension of needing 5 missing `protection-*-active` conditions (force/necrotic/poison/psychic/radiant) plus the ConsumeAction wire.~~ |
| ~~Amulet of Health / Gauntlets of Ogre Power / Belt of *Giant Strength~~ | ~~slice 224~~ | ~~Closed by slice 229: `OverrideAbilityScore` primitive (floor semantics, mirrors SetACFloor). 8 items wired in the same slice. Belt of Dwarvenkind's other arms (Resistance, Darkvision, Advantage save vs poison) still defer to separate rows.~~ |
| ~~Audit: variant-unroll rarity validation (drift backfill)~~ | ~~slice 229~~ | ~~Closed by slice 235 side-cleanup: Belt of Hill Giant Strength rare → uncommon, Stone/Frost/Fire → rare, Cloud → very-rare per SRD. The underlying audit-script limitation (variant names don't match SRD parents, so the rarity check silently skips) remains; a future audit extension could add variant-rollup awareness, but the canonical drifts that prompted this row are fixed.~~ |
| ~~Mantle of Spell Resistance — Advantage on saves vs spells~~ | ~~slice 224~~ | ~~Closed by slice 258: `SetAdvantage.condition` predicate honored in the effect-stack builder (previously documented in the schema but silently dropped); new `event.isSpellSave` fact populated by `computeSavingThrow` from `input.sourceIsMagical`; Mantle wired with 6 SetAdvantage entries (one per ability score), each gated on `eq path:'event.isSpellSave' value:true`. The fact reflects "spell or magical source" since the engine doesn't distinguish spell from non-spell magical effects today — conservatively grants advantage on a broader set of saves than strict RAW (more saves benefit, never fewer; matches slice 131's Magic Resistance handling).~~ |
| ~~`SetAdvantage` siblings still drop their `condition` field~~ | ~~slice 258~~ | ~~Closed by slice 262 (pattern-check norm applied per slice 261): the three sibling effect kinds (`GrantResistance`, `ModifyActionEconomy`, `GrantAdvantageToAttackers`) now thread `effect.condition` through the builder. `GrantResistance` gained a `predicate?` field on existing entries; `ModifyActionEconomy` gained a parallel `predicatedActionEconomy` map alongside the unconditional totals; `GrantAdvantageToAttackers` gained a `predicatedAdvantageToAttackers` entries list alongside the unconditional flag. All three readers now accept optional facts and merge unconditional + filtered predicated. Pure plumbing slice — 0 pack entries set `condition` on these kinds today; future canonical users wire as one-line content edits.~~ |
| ~~Wings of Flying — activate-as-action toggle item~~ | ~~slice 224~~ | ~~Closed by slice 240: new `UseAction` discriminated union (ApplyCondition variant) + `onUse: UseAction[]` field on MagicItemSchema + `planUseItem` planner + `ItemUsed` event. Wings of Flying wired as canonical user (`charges: 1/dawn` + `onUse: [{ kind: 'ApplyCondition', conditionId: 'flying-active' }]`). The Boots of Speed / Levitation / Cloak of the Bat siblings still need `CastSpell` and `Toggle` `UseAction` variants.~~ |
| ~~Boots of Speed — click-on / click-off toggle~~ | ~~slice 240~~ | ~~Closed by slice 242: new `Toggle` UseAction variant + planUseItem branch (apply if condition absent, remove if present). Wired with new content-side `boots-of-speed-active` condition (ModifySpeed walk × 2). RAW "Disadvantage on opportunity attacks against wearer" arm + cumulative 10-minute-per-long-rest budget still deferred (see rows below).~~ |
| ~~Boots of Speed — Disadvantage on opportunity attacks against wearer~~ | ~~slice 242~~ | ~~Closed by slice 269: `event.isOpportunityAttack` predicate fact had already landed in slice 206 (for Hunter L7 Escape the Horde, the first canonical user of `ImposeDisadvantageOnAttackers` gated on opportunity-attack-ness). The boots-of-speed-active condition was the second canonical user waiting on it; the row was stale for 27 slices. Pattern-check meta-finding: when a slice adds a new predicate fact + one canonical user, sweep the deferred-primitives backlog for other content awaiting the same fact.~~ |
| ~~Frightened condition — dual bug: narrow ability checks + missing source-in-LoS gate~~ | ~~slice 264~~ | ~~Closed by slice 276: both axes fixed simultaneously. Breadth axis: existing per-ability `SetAdvantage on:{kind:'check', ability:'STR'}` replaced with the slice-266 check wildcard `SetAdvantage on:{kind:'check'}` so all 6 abilities take disadvantage. LoS axis: new `bearer.canSeeFearSource` consumer-supplied predicate fact on `ComputeAbilityCheckInput` + `AttackIntent`. Both arms (attack disadvantage + check wildcard disadvantage) gate on `not eq path:'bearer.canSeeFearSource' value:false` — default-apply semantics: undefined or true fires (matches current behavior for consumers that don't model LoS); only an explicit `false` from the consumer bypasses. Mirrors the slice-263 `sense?` / slice-274 `athleticsSubAction?` pattern of consumer-supplied scene state. The fact is per-bearer (any source of fear in LoS produces true); per-source granularity stays deferred.~~ |
| ~~Dodged condition — `ImposeDisadvantageOnAttackers` missing LoS gate~~ | ~~slice 267 (meta-finding: slice 264 sweep was too narrow)~~ | ~~Closed by slice 278: new `targetCanSeeAttacker?: boolean` field on `AttackIntent` and `ResolveAttackInput`, threaded into the `attackerFacts` map as `bearer.canSeeAttacker`. Per-attacker fact (different from slice 276's per-bearer `bearer.canSeeFearSource`): the same dodging creature might see attacker A but not attacker B. The dodged condition's `ImposeDisadvantageOnAttackers` gate combined with slice 272's Incap/Speed-0 gate via `all`. Default-apply semantics (predicate is `not eq value:false`); consumers that model LoS supply `false` to bypass when the target can't see the attacker. The DEX-save advantage arm stays gated only on Incap/Speed-0 per RAW (no LoS clause on that benefit).~~ |
| ~~Dodged condition — both arms missing Incapacitated / Speed-0 disabler~~ | ~~slice 267 (meta-finding: slice 264 sweep was too narrow)~~ | ~~Closed by slice 272 via path (a) per-effect `condition` predicate: new `bearer.hasIncapacitated` + `bearer.speedZero` boolean facts populated in [src/engine/plan/attack.ts](../src/engine/plan/attack.ts) and [src/derive/save.ts](../src/derive/save.ts) (the two consumer call sites for the dodged effect entries). Both arms of the dodged condition gate on `all(bearer.hasIncapacitated=false, bearer.speedZero=false)` via the existing slice-103 / slice-258 predicate plumbing. `findActorBlockingCondition` (which already accounts for Incapacitated + Stunned/Paralyzed/Petrified/Unconscious all RAW-include-Incapacitated, plus HP<=0) supplies the hasIncapacitated boolean; `getEffectiveSpeed` supplies the speedZero check. Path (b) (condition-level `disabledWhile?` field) deferred as not yet justified by a second canonical user; Dodge is the only 2024 RAW condition with this shape today.~~ |
| ~~Blurred-active condition — `ImposeDisadvantageOnAttackers` missing attacker-sense bypass~~ | ~~slice 267 (meta-finding: slice 264 sweep was too narrow)~~ | ~~Closed by slice 271: new `attacker.bypassesSightIllusion` boolean fact populated in the attack planner's `attackerFacts` map (mirrors slice 127's Mirror Image bypass logic: true when attacker has blindsight, tremorsense, or truesight, or carries the Blinded condition). The `blurred-active` ImposeDisadvantageOnAttackers entry gates on `eq path:'attacker.bypassesSightIllusion' value:false`. Pattern-check sweep (per slice 261 norm) walked all 8 ImposeDisadvantageOnAttackers wires in the pack; Blur was the only same-shape bug (Mirror Image already had a dedicated bypass; Foresight is universally-correct per RAW; Holy Aura is universal per RAW; type-conditional wards gate on attacker type not sense; Dodged has its own deferred row).~~ |
| ~~Invisible condition — missing both halves of the visibility-bypass shape~~ | ~~slice 271 (pattern-check sibling of slice-271 Blurred fix)~~ | ~~Closed by slice 273: new `attacker.canLocateInvisible` + `target.canLocateInvisible` symmetric boolean facts populated in the attack planner (true when the counter-party has blindsight, tremorsense, or truesight). Distinct from slice-271's `attacker.bypassesSightIllusion` because the Blinded condition does NOT bypass invisibility (a blinded creature can't see anything). Invisible's existing SetAdvantage on attack now gates on `target.canLocateInvisible=false`; a new ImposeDisadvantageOnAttackers entry gates on `attacker.canLocateInvisible=false`. `attackerEffects.advantageFor('attack')` now receives a facts map carrying `target.canLocateInvisible`.~~ |
| ~~Boots of Speed — cumulative 10-minute-per-long-rest budget~~ | ~~slice 242~~ | ~~Closed by slice 293: new `timeBudget: { maxMinutesPerLongRest }` field on MagicItemSchema plus a `minutesUsed?: number` counter on ItemInstance. New `ItemTimeBudgetConsumed` event. UseItemIntent gains an optional `minutesElapsed?: number` field; the Toggle branch of `planUseItem` validates `minutesUsed < max` on toggle-on (throws when exhausted: "the magic ceases to function until you finish a Long Rest") and emits `ItemTimeBudgetConsumed` on toggle-off when the consumer reports elapsed minutes. `applyLongRestEnded` walks each participant's inventory and resets `minutesUsed` to 0 on instances that carry the counter. Distinct from `charges` (per-use integer count) and round-based auto-expiry (slice 102): a continuous time budget. Same shape Winged Boots needs (still wires-only).~~ |
| ~~Boots of Levitation — at-will Levitate on the wearer~~ | ~~slice 240~~ | ~~Closed by slice 241: new `CastSpell` variant on UseAction + planUseItem branch delegating to planCastSpell with noSlotCost + ignorePreparation. Wired with `{ spellId: 'levitate', slotLevel: 2, castingClassId: 'wizard' }`. Levitate is schema-only so the cast records SpellCastDeclared but no mechanical chain.~~ |
| ~~Hat of Disguise — at-will Disguise Self on the wearer~~ | ~~slice 240~~ | ~~Closed by slice 241 alongside Boots of Levitation. Wired with `{ spellId: 'disguise-self', slotLevel: 1, castingClassId: 'wizard' }`. Same schema-only-cast caveat.~~ |
| Cloak of the Bat — fly speed + advantage on Stealth in dim light or darkness | slice 240 | `CastSpell` UseAction is in place (slice 241), but Cloak of the Bat's Polymorph-to-Bat arm specifically uses the dedicated planPolymorph (deferral parallel to slice-237's Spell Scroll of Misty Step / Wish). Plus the fly speed + Stealth-advantage arms still need the slice-227 light-level predicate. |
| ~~Helm of Telepathy / Decanter of Endless Water — spell-grant items~~ | ~~slice 240~~ | ~~Closed by slice 243's action-selector + fixed chargesCost: Helm of Telepathy's Detect Thoughts (1 charge) + Suggestion (1/dawn) split now wires as a 2-action item with `actionId` per arm. Decanter of Endless Water's three command words wire as 3 actions with distinct `actionId` per command. (Authoring these specifically still pending, but the engine shape exists.)~~ |
| ~~Wand of Magic Missiles / Wand of Fireballs / Wand of Lightning Bolts — variable charge cost~~ | ~~slice 241~~ | ~~Primitive shipped slice 253 (`chargesCostMax?: number` on the CastSpell UseAction variant + `chargesCost?: number` dial on UseItemIntent; planner computes effective slot as `slotLevel + (intent.chargesCost - action.chargesCost)`). All three wands wired: Magic Missiles in slice 253 (1-3 charges → L1-L3); Fireballs + Lightning Bolts in slice 255 (1-3 charges → L3-L5; SRD caps charges-per-use at 3, not 7).~~ |
| ~~Staff of Healing — variable-cost Cure Wounds arm (1-4 charges → slot 1-4)~~ | ~~slice 243~~ | ~~Wired in slice 255: appended `{ kind: 'CastSpell', actionId: 'cure-wounds', spellId: 'cure-wounds', slotLevel: 1, chargesCost: 1, chargesCostMax: 4, castingClassId: 'cleric' }` to Staff of Healing's onUse array. Now all three Staff of Healing arms (Cure Wounds + Lesser Restoration + Mass Cure Wounds) wire.~~ |
| ~~Staff of Healing — "expend the last charge, roll 1d20; on 1 the staff vanishes"~~ | ~~slice 243~~ | ~~Primitive shipped slice 256: new `destructionRoll: { trigger: 'lastChargeExpended', die, destroyOn }` field on MagicItemSchema + new `ItemDestroyed` event + `applyItemDestroyed` reducer + `rollDie` baked into the emitted event so apply() stays RNG-free. planUseItem fires the roll after the final charge is consumed; if the result is in `destroyOn`, the instance retires. Wand of Magic Missiles wired in slice 256; Wand of Fireballs + Wand of Lightning Bolts + Staff of Healing wired in slice 257 (identical shape). Wind Fan's 20% per-use tear is a different trigger (`eachUse`) and stays deferred until that variant lands.~~ |
| ~~Pipes of Haunting — DC 15 WIS save vs Frightened~~ | ~~slice 241~~ | ~~Closed by slice 286: new `Save` UseAction variant on the slice-240 UseActionSchema. Distinct from CastSpell (no spell is cast — the item has its own bespoke save mechanic at a fixed DC). Variant fields: `saveAbility`, `saveDC`, `conditionOnFail`, `sourceIsMagical?`. planUseItem rolls one save per target in the new `saveTargetIds` field on UseItemIntent and emits SaveRolled + (on fail) ConditionApplied. Also fixed the recharge formula from a pre-285 stub of `1d4+1` to the RAW `1d3`.~~ RAW deviations: 30-ft scope is consumer territory (engine doesn't model positions); 1-minute duration is consumer-managed (mirror of slice 236's ApplyCondition doc); end-of-turn recurring save and 24-hour immunity-on-success are still deferred (would need a `recurringSave` shape applied via the planner and consumer-tracked per-target immunity state). |
| ~~Most consumables (potions, oils, dusts, scrolls in pack)~~ | ~~slice 224~~ | ~~Primitive shipped in slice 235: new `ConsumeAction` discriminated union + `planConsumeItem` planner + `ItemConsumed` event. Heal action wired; Potions of Healing × 4 functional. Future ConsumeAction extensions (ApplyCondition, CastSpell) unblock the rest of the consumable catalog as content-only follow-ups.~~ |
| ~~Bracers of Archery — +2 damage bonus with Longbow/Shortbow specifically~~ | ~~slice 224~~ | ~~Closed by slice 275 (damage arm): new `event.weaponId` predicate fact on the attack planner's `damageFacts` map. Bracers of Archery's `AddModifier { target: 'damage', value: 2 }` gates on `any(eq path:event.weaponId value:longbow, eq path:event.weaponId value:shortbow)`. The proficiency arm (RAW: grants longbow/shortbow proficiency regardless of class) stays deferred until a conditional `GrantProficiency` shape lands — most archery-focused builds already carry these proficiencies via class, so the damage arm is the higher-payoff mechanical wire. The new `event.weaponId` fact unblocks any future weapon-specific item buff with the same shape.~~ |
| ~~Potion of Heroism — flat 10 temp HP + Bless effect~~ | ~~slice 239~~ | ~~Closed by slice 282: new `GrantTempHP { amount }` ConsumeAction variant (sibling of slice-235 Heal); planConsumeItem emits a TempHPGranted event consuming the existing slice-75 applyTempHPGranted reducer's max-not-additive semantics. The Bless half wires via the existing slice-236 ApplyCondition variant pointing at the pre-existing `blessed` condition. The 1-hour duration is consumer-managed per the ConsumeAction doc comment.~~ |
| ~~Potion of Resistance — chooser-at-creation damage type~~ | ~~slice 239~~ | ~~Closed by slice 296 via variant unroll (siblings of slice 295's Armor / Ring of Resistance unroll, same RAW d10 table): 10 `potion-of-resistance-<type>` consumable variants, each with a single ApplyCondition ConsumeAction pointing at the matching `protection-<type>-active` condition. The 5 missing conditions (force, necrotic, poison, psychic, radiant) shipped in the same slice — each is a single GrantResistance for that damage type, mirroring the existing 5 (acid, cold, fire, lightning, thunder) from Protection from Energy. Protection from Energy's RAW spell scope (only the 5 elemental types) is unchanged. The 1-hour duration is consumer-managed (mirror of slice 236's ApplyCondition doc).~~ |
| Potion of Fire Breath — fire AoE-line breath, three uses within 1 hour | slice 239 | New `AoEDamage { dice, damageType, save, halfOnSuccess }` `ConsumeAction` variant. The three-uses-within-an-hour shape needs the same per-item charges + duration that AT-WILL items use (Cap of Water Breathing's `UseItem` row). |
| Potion of Poison — CON save 3d6 poison + 1h Poisoned | slice 239 | Combined save + damage + condition `ConsumeAction` variant (or compose three new variants and emit them per-action). The bare Poisoned condition already exists; the engine just lacks the save-then-damage-and-condition path inside consume. |
| ~~Potion of Vitality — remove Exhaustion + end Poisoned~~; 24h max-hit-die spend | ~~slice 239~~ + slice 283 (HD-spend rider still deferred) | ~~First arm closed by slice 283: two new ConsumeAction variants. `RemoveConditions { conditionIds: string[] }` walks the bearer's `appliedConditions` and emits ConditionRemoved per matched instance (no-op for ids not present). `RemoveExhaustion {}` emits ExhaustionChanged from current → 0 when current > 0 (no-op when already zero). Potion of Vitality wires both: `[RemoveExhaustion, RemoveConditions { conditionIds: ['poisoned'] }]`.~~ The 24h max-HD-spend half is still deferred — a Hit Die-spend rider that doesn't exist today (would compose with a future Hit Die spend resolver). |
| ~~Antitoxin — advantage on saves vs Poisoned for 1 hour~~ | ~~slice 239~~ | ~~Closed by slice 291: new `savePreventsCondition?: string` field on `ComputeSaveInput` (surfaces as `event.savePreventsCondition` predicate fact). Cast-spell save mechanic populates it from `mechanic.conditionOnFail` (e.g. Poison Spray, Stinking Cloud's poisoned-on-fail save). Recurring-save planner populates it from the bearer condition id when `recurringSave.onSuccess === 'removeCondition'` (Hold Person, etc. — though Antitoxin's "or end" arm only fires on poisoned-shaped recurring saves; today no such condition exists, but the wire is in place for future content). New `antitoxin-active` condition uses the slice-266 save wildcard + `eq event.savePreventsCondition 'poisoned'` predicate. The 1-hour duration is consumer-managed (mirror of slice 236).~~ |
| ~~Oil of Sharpness — +3 to attack / damage, weapon counts as magical, 1 hour~~ | ~~slice 239~~ | ~~Closed by slice 284: new `ApplyItemBuff { attackBonus?, damageBonus?, extraDamageDice?, extraDamageType? }` ConsumeAction variant composes with slice-76's ItemInstance.temporaryBuff. The "counts as magical" arm is free (slice 112's isMagicWeaponAttack returns true for any temporaryBuff-bearing item). RAW deviations: engine doesn't gate on weapon type (piercing/slashing only) or auto-expire after 1 hour (consumer-managed). Same variant also closes Poison Basic (extra-1d4-poison rider).~~ |
| Holy Water / Acid / Alchemist's Fire — thrown attack from inventory | slice 239 | New `ThrowAttack { range, damageDice?, damageType?, save?, conditionalDamageOnCreatureType? }` `ConsumeAction` variant. Holy Water needs the creature-type gate (Fiend / Undead) the engine already supports via creatureType predicates. |
| Ball Bearings / Caltrops — area-placement difficult terrain | slice 239 | New `PlaceTerrain { area, terrainKind, save?, condition? }` `ConsumeAction` variant. Engine doesn't model positions; the wire would be a discoverable annotation the consumer surfaces on a tile map. |
| ~~Poison Basic — apply poison to weapon, next hit deals +1d4 poison~~ + DC 10 CON save vs Poisoned | ~~slice 239~~ + slice 284 (save-rider still deferred) | ~~Extra-damage arm closed by slice 284 via the shared ApplyItemBuff ConsumeAction variant: stamps `temporaryBuff: { extraDamageDice: '1d4', extraDamageType: 'poison' }` on the target weapon, reusing slice-90's elemental-rider attack-planner path.~~ The DC 10 CON save vs Poisoned arm stays deferred — the slice-235 ConsumeAction surface doesn't yet compose with on-hit saves; future work would extend the temporaryBuff shape with an on-hit-rider slot or compose with the slice-61 on-hit-rider system. |
| ~~Perfume — advantage on Persuasion checks for 1 hour~~ | ~~slice 239~~ | ~~Closed by slice 292: pure content slice on top of existing primitives. The original row miscategorized the gap — the skill-discriminated SetAdvantage target `on: { kind: 'skill', skill: Skill }` had been in the schema since slices 263 / 274 (Eyes of the Eagle Perception, Gloves of Swimming Athletics, Cloak of the Bat Stealth). Perfume now ships as the canonical Persuasion user via slice-236 ApplyCondition + new `perfumed-active` condition with `SetAdvantage on:{kind:'skill', skill:'persuasion'}`. RAW deviations: Indifferent Humanoid target-attitude gate is consumer-managed (engine doesn't model attitude); 5-ft range and 1-hour duration are consumer-managed.~~ |
| ~~Doc-size CI check for front-door docs~~ | ~~slice 270 (recurring)~~ | ~~Closed by slice 285: new [tests/audit/doc-size.test.ts](../tests/audit/doc-size.test.ts) audit asserts every front-door doc (README.md, CHANGELOG.md, CLAUDE.md, docs/starter-pack-gaps.md, docs/status.md, docs/roadmap.md, docs/api-overview.md) plus every docs/changelog/*.md archive and docs/gaps-*.md catalog is <= 60,000 bytes. 60 KB matches CLAUDE.md's "Doc size discipline" documented hard ceiling; empirical verification at slice 285 (59 KB archives Read cleanly; 65 KB CHANGELOG pre-archive failed). Runs as part of `npm test` so CI catches drift at commit time rather than at next-agent-Read.~~ |
| ~~Consumer-half tracking for engine-half-only RAW fixes~~ | ~~slice 276 (introduced consumer-coordinated pattern)~~ | ~~Closed by slice 294: see the new "Consumer-coordinated fact slots" section below. Catalogs each engine-half-landed slot + its entry-point inputs + default-undefined semantic + what's gated. Future LoS / scene-state fixes append to that table at the close of the introducing slice.~~ |
| ~~AddModifier save/check wildcard (slice-266 sibling for AddModifier kind)~~ | ~~slice 298 (pattern-check finding)~~ | ~~Closed by slice 299: extended `ModifierTargetSchema` so `ability` is optional on save/check kinds (mirror of slice-266 RollTarget). New `modifierWildcardKeyFor` + reader updates in `modifierSum` / `modifierBreakdown` merge `save:*` / `check:*` bucket entries into per-ability queries. Refactored 6 unrolled wires (Stone of Good Luck 12→2; Cloak/Ring of Protection 7→2; blessed/baned 7→2; aura-of-protection-active 6→1). Stone of Good Luck (Luckstone) is the canonical user; sibling cleanups bundle in the same slice. Net pack content: 36 entries → 6 (effective). Also surfaced a pre-existing RAW deviation in Bless/Bane (flat +2/-2 vs RAW 1d4) which stays deferred as its own row (slice-299 refactor preserved the existing approximation).~~ |
| Bless / Bane flat +2 / -2 vs RAW 1d4 | slice 299 (surfaced during AddModifier wildcard refactor) | RAW (SRD 5.2.1) for both Bless and Bane: "add (or subtract) 1d4 from the attack roll or save." Pack approximates as a flat +2 / -2 (the 2014 / pre-2024 fixed value, documented in [tests/unit/rules-truth.test.ts:262](../tests/unit/rules-truth.test.ts). The slice-299 wildcard refactor preserved the approximation. Fixing properly needs a per-roll bonus die primitive distinct from AddModifier's static `value` field — possibly `AddBonusDie { target, dice }` that rolls fresh on each affected roll, baked into the planner's emitted event so apply() stays RNG-free. Canonical users on land: Bless + Bane. Future siblings (Guidance + 1d4 to one check; Resistance + 1d4 to one save; Bardic Inspiration + d6/d8/d10/d12 to one roll) would all use the same primitive. |
| Warding Bond damage-sharing arm | slice 301 | RAW: "each time [the target] takes damage, you take the same amount of damage." Engine has no shared-damage-link primitive: every primary damage emitter would need to project a paired DamageApplied onto the linked source character. Distinct from concentration-aura damage (which is fixed-amount AoE) and from on-hit triggers (which fire on hit not on damage). Three remaining arms wired in slice 301 (+1 AC, +1 save wildcard, Resistance to all); this fourth arm + the 60-ft proximity gate (consumer-managed for now) round out the spell. |
| ~~Dead 2014-era orphan conditions (no SRD 5.2.1 spell carrier)~~ | ~~slice 301 (pattern-check during buff-spell sweep)~~ | ~~Closed by slice 304: removed all six dead conditions (`wrathful-smite-active`, `thunderous-smite-active`, `branding-smite-active`, `holy-weapon-active`, `invulnerable-active`, `earthbound-active`) from the pack — none had an SRD 5.2.1 spell carrier (all 2014 PHB leftovers). The `effective-non-walk-speed.test.ts` "zero-set wins" case that leaned on `earthbound-active` now uses a test-local fly-set-0 fixture condition (kept out of the shipped pack). `KNOWN_DEAD_ORPHANS` in [tests/audit/pack-integrity.test.ts](../tests/audit/pack-integrity.test.ts) emptied; the audit now enforces zero orphans. Coverage snapshot dropped the 6 conditions.~~ |

When a slice ships one of these unblockers, walk the affected rows and either delete them (closed) or move them to a new bucket. The goal is for this list to stay short — every entry is concrete enough to be the canonical user for a focused primitive slice.

## Consumer-coordinated fact slots (engine half landed, consumer half pending)

The engine has accreted a small set of optional input fields that consumers populate from scene state for certain RAW gates to fire. Each is a deliberate split: the engine ships the predicate plumbing + the wire on the affected condition / item; the consumer (dndbnb's encounter manager, a web demo, a future VTT) supplies the per-intent value. Where the value is undefined, two semantic flavors apply (see "Why two semantic flavors" below).

| Fact slot | Type | Entry points | Default-undefined semantic | What's gated | Added |
|---|---|---|---|---|---|
| `bearerCanSeeFearSource` | `boolean` | `AttackIntent` ([src/engine/plan/attack.ts](../src/engine/plan/attack.ts) `planAttack`), `ResolveAttackInput` ([src/engine/plan/attack.ts](../src/engine/plan/attack.ts) `resolveAttack`), `ComputeAbilityCheckInput` ([src/derive/ability-check.ts](../src/derive/ability-check.ts) `computeAbilityCheck`) | default-apply (`undefined` matches `true` for the predicate `not eq value:false`) | Frightened condition's attack-disadvantage + check-wildcard-disadvantage arms ([Frightened RAW: "Disadvantage on ability checks and attack rolls while the source of fear is within line of sight"](../src/content/packs/starter-pack.json)). Consumer supplies `false` only when the bearer cannot see any source of fear; everything else keeps the strict-RAW default. | slice 276 |
| `targetCanSeeAttacker` | `boolean` | `AttackIntent` (planAttack), `ResolveAttackInput` (resolveAttack) | default-apply (`undefined` matches `true`) | Dodge condition's `ImposeDisadvantageOnAttackers` (attack-disadvantage arm only; the DEX-save advantage arm is unaffected per RAW). Per-attacker (not per-bearer): the same dodging creature might see attacker A but not attacker B. Consumer supplies `false` only when the target cannot see this specific attacker. | slice 278 |
| `lightLevel` | `'bright' \| 'dim' \| 'darkness'` | `ComputeAbilityCheckInput` (computeAbilityCheck) | opt-in (predicates that require a specific value evaluate false when undefined; bearer must explicitly receive the value to gain the gated benefit) | Cloak of the Bat Stealth advantage (gates on `bearer.lightLevel ∈ {dim, darkness}`). Same opt-in shape as slices 263 (`sense?`) and 274 (`athleticsSubAction?`): a specific narrative context is required, and the absence of scene state produces no benefit. | slice 279 |

**Consumer call sites.** The values are computed from scene state at intent-construction time:
- `bearerCanSeeFearSource` -> scan the bearer's known fear sources (any creature within range carrying a fear-source role) for line of sight to the bearer. Multi-source aggregation is OR; per-source granularity (which specific source is visible) stays deferred.
- `targetCanSeeAttacker` -> direct LoS check from the dodging target to the resolved attacker.
- `lightLevel` -> ambient light value at the bearer's current tile. The 3-value enum matches the 2024 PHB / DMG light-tier vocabulary; any future "magical darkness" / "twilight" tiers would extend the enum.

**Until a consumer wires the population**, the gated RAW behaviors observe these defaults:
- Frightened applies disadvantage to attacks + checks in all cases (default-apply matches the prior-to-slice-276 behavior; not a regression).
- Dodge's attack-disadvantage applies against every attacker (default-apply matches the prior-to-slice-278 behavior; not a regression).
- Cloak of the Bat's Stealth advantage never fires (opt-in; the prior-to-slice-279 broader-than-RAW behavior is replaced by strict RAW once a consumer wires `lightLevel`).

**Why two semantic flavors.** Default-apply is the right fit when the RAW arm describes a *penalty* the bearer suffers under a typical condition ("Frightened applies disadvantage unless ...") — the predicate's `not eq value:false` shape leaves prior behavior intact and lets consumers opt out per intent. Opt-in is the right fit when the RAW arm describes a *benefit* gated on a specific narrative context ("Stealth advantage while in dim light or darkness") — the bearer must explicitly receive the value to gain the benefit, matching the slice-263 `sense?` / slice-274 `athleticsSubAction?` pattern. When adding new consumer-coordinated facts, mirror the semantic that matches RAW intent.

**Appending to this table.** When a future slice adds a new engine-half slot, append a row at the close of the introducing slice (alongside the CHANGELOG entry + the deferred-primitives-backlog closure if applicable). Keep the entry-point list grounded in actual file paths so future agents can grep their way from a row to the affected sites.

## How this list is maintained

At the close of each content slice, update the relevant section here and bump the "Coverage at a glance" counts. If the slice introduces a new mechanic kind (e.g. a future reaction-spell primitive), retro-update the affected schema-only spells to either `wired` or move them to a different deferred bucket. When an engine slice from the "Future engine slices" table ships, mark it as done and walk the affected spells in this doc to their new status.

**Also add to the "Deferred primitives backlog" above** any new deferral noted in a slice commit body: if a slice ships with documented approximations or RAW deviations, those go in the backlog so the next session has a concrete priority queue instead of having to grep CHANGELOG history.
