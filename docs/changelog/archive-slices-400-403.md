# CHANGELOG archive: slices 400-403 (content-pack separation cohort)

Per-slice detail for slices 400-403, moved out of the live [CHANGELOG.md](../../CHANGELOG.md) in slice 404 to keep it under the 60 KB single-Read ceiling. Cohort: the multi-pack id-collision policy + pack validator (400), then the full SRD/non-SRD content-pack separation, backgrounds + feats (401), the 12 spells + their conditions (402), and finally removing non-SRD content from shipped/tracked source into the gitignored content-packs/ folder (403). See also [archive-slices-392-397.md](archive-slices-392-397.md).

---

**Content (slice 403): stop shipping non-SRD content; move it to a gitignored `content-packs/` folder**

Slices 401-402 separated the non-SRD content into its own pack but still *shipped* it in the repo. This removes it from tracked/shipped source entirely: the distributed package and the tracked repo are now SRD-only. Verified the extras content is mechanics-only (zero verbatim WotC flavor text; the only prose is the engine's own mechanical paraphrase on 6 conditions), so the legal exposure was low, but removing it is consistent with the alpha.0-5 IP-cleanup and the "schema-only library; consumers supply content packs" design.

Deleted `src/content/packs/extras.ts` + the `loadPhbExtrasPack` / `PHB_EXTRAS_PACK_RAW` exports; the full `phb-2024-extras.json` moved to a new gitignored `content-packs/` folder (tracked [content-packs/README.md](../../content-packs/README.md) carries pack-authoring + loading instructions). The engine's planner/feature tests genuinely need some of those mechanics (absorb-elements / thunder-step / elemental-weapon planners, Dueling/Protection fighting styles), so the minimal tested subset (12 spells + 6 conditions + 2 feats, mechanics-only, **not** in the npm package) moved to a tracked test fixture [tests/fixtures/content/phb-extras-test-pack.json](../../tests/fixtures/content/phb-extras-test-pack.json) with a `loadPhbExtrasTestPack()` helper. The 15 tests that cast these spells now load `[loadStarterPack(), loadPhbExtrasTestPack()]`. The feature-coverage matrix reads the fixture for the tested surface, and its completeness checks were reframed to SRD reality (the 4 SRD fighting styles + 7 SRD epic boons; the wired-feat snapshot dropped Lucky + Unarmored Defense, the only two wired non-SRD feats no longer loadable in tests). Contract snapshot dropped the two removed exports.

Docs: content-attribution.md, status.md, getting-started.md, starter-pack-gaps.md reframed from "in the phb-2024-extras pack / loadPhbExtrasPack()" to "user-supplied in the gitignored content-packs/ folder." doc-counts unchanged (the starter pack didn't change in 403). No engine logic change. No em/en dashes. `tsc --noEmit` clean; full suite green (332 files / 2170 passing).

**Content (slice 402): move the 12 non-SRD spells (+ their 6 conditions) into `phb-2024-extras`**

The entangled half of the SRD/non-SRD pack split (slice 401 did backgrounds + feats). Moved the 12 wired non-SRD spells (XGE/TCE: Absorb Elements, Armor of Agathys, Blade Ward, Cause Fear, Cloud of Daggers, Cordon of Arrows, Crusader's Mantle, Elemental Weapon, Hunger of Hadar, Spirit Shroud, Summon Beast, Thunder Step) into the extras pack, plus the 6 conditions only those spells apply (armor-of-agathys-active, blade-warded-active, crusaders-mantle-active, spirit-shroud-{cold,necrotic,radiant}-active). **`loadStarterPack()` is now 100% SRD-derived** across every category (339 spells, 124 conditions, 4 backgrounds, 18 feats); the srd-drift audit covers the whole starter pack with nothing to skip.

The planners (reactive-spells, thunder-step in movement, elemental-weapon, the summon/trap/aura mechanics) are generic engine code and stayed put; they resolve their spells from whatever packs are loaded. Entanglement was the predicted cost: 15 test files cast these spells and now load `[loadStarterPack(), loadPhbExtrasPack()]`; the feature-coverage matrix's wired-feat and wired-condition catalogs read both packs (they assert full shipped capability, so the snapshots stay complete with no churn). The 6 conditions had to move with their spells or pack-integrity flags them as orphans (a condition applied only by a now-absent spell), which is exactly what the audit caught, confirming the move was necessary.

Doc reconciliation: gaps-spells.md per-level headers + bodies + the wired sub-counts (339 in pack: 182 wired / 70 narrative / 87 deferred; cast-time 147, zone-tick 11, planner 24); doc-counts (getting-started.md spells 351->339 + conditions 130->124); status.md spell + condition rows; starter-pack-gaps.md; content-attribution.md (spells now 100% SRD in the distributed pack). gaps-spells-counts + doc-counts + pack-integrity + spell-coverage all green. No engine change. No em/en dashes. `tsc --noEmit` clean; full suite green (332 files / 2170 passing).

**Content (slice 401): extract PHB-2024 backgrounds + feats into a separate `phb-2024-extras` pack**

First real use of the slice-400 multi-pack architecture. The starter pack shipped 15 PHB-2024 backgrounds and 17 PHB-2024 feats outside the SRD 5.2.1 license envelope (kept since slice 152 "for character-creation breadth"). Moved them into a new opt-in [phb-2024-extras.json](../../src/content/packs/phb-2024-extras.json) pack with `loadPhbExtrasPack()` / `PHB_EXTRAS_PACK_RAW`, so `loadStarterPack()` is now **purely SRD-derived** (4 backgrounds: Acolyte/Criminal/Sage/Soldier; 18 SRD feats). This tightens the IP story (the distributed starter pack is now 100% CC-BY SRD) and means the srd-drift audit covers the whole starter pack with no non-SRD entries to skip. Consumers wanting the full character-creation breadth load both packs: `createEngine({ contentPacks: [loadStarterPack(), loadPhbExtrasPack()] })`.

The starter-pack.json edit is a surgical removal (46 deletions, 1 insertion: the trailing-comma fixup on the new last background) preserving every kept entry's exact formatting. Entanglement was lighter than the spell side: the moved backgrounds/feats are referenced only in the pack JSON, except the Dueling + Protection fighting-style feats, which two planner tests exercise; those tests now load both packs. The feature-coverage matrix reads both packs (it asserts the full shipped capability, so the wired-feat snapshot stays complete). The starter-pack content test keeps its SRD-only scope with thresholds lowered to the SRD counts.

This reverses the slice-152 "keep extras in, let strict consumers prune" policy (documented in [content-attribution.md](../content-attribution.md), now updated): the default is SRD-only and breadth is opt-in. Spells are the next, more entangled half (the 12 wired non-SRD spells have dedicated planners + tests). doc-counts reconciled (getting-started.md: 18 feats, 4 backgrounds); status.md + starter-pack-gaps.md + content-attribution.md updated. No engine change. No em/en dashes. `tsc --noEmit` clean; full suite green (332 files / 2177 passing).

**Engine (slice 400): multi-pack id-collision policy + a pack validator (content-authoring foundation)**

Groundwork before any volume content authoring beyond SRD. SRD pack-presence is already 100% complete, so new content (homebrew, non-SRD, campaign catalogs) belongs in a *separate* pack the engine loads alongside the SRD pack (`createEngine` already takes `ReadonlyArray<ContentPack>`; `resolveContent` merges them). The gap was the merge itself: `resolveContent` did `map.set(e.id, e)` last-wins, so a second pack could **silently clobber** an SRD entry on an accidental id reuse, with no error.

Now `resolveContent` throws a `ContentPackLoadError` on any within-pack duplicate id or any cross-pack collision, naming the id, category, and prior pack. A later pack may *intentionally* replace an earlier id only by declaring it in a new optional `overrides: string[]` field (a deliberate houserule); the later entry then wins. Chose code-enforced collision detection + explicit overrides over an id-prefix namespacing convention, because namespacing would break legitimate cross-pack references (a homebrew subclass's `parentClassId: 'fighter'`) and can't be enforced, only hoped for.

The pack validator is `validatePacks(packs)`: the report-all author-time companion that returns *every* id collision plus *every* dangling cross-reference (across the merged set), without throwing, so a pack author sees all problems at once. It builds on the existing `validateCrossReferences`, which this slice generalized from two hand-written checks (originFeatId, parentClassId) to a deep-walk over all 10 id-reference fields (spellId, conditionId, conditionOnFail/OnSuccess, allyConditionId, applyConditionId, bearerConditionId, enchantmentDefinitionId, plus the original two), each resolving against its target category with the existing Levenshtein "did you mean" suggestion. New public exports: `validatePacks`, `detectIdCollisions`, `mergeContent`, `ContentPackIssue`.

**Found a real latent bug:** the new collision check immediately caught the `TEST_PACK` fixture shipping the `shield` armor item *twice* (identical entries), exactly the silent-clobber the policy prevents. Removed the duplicate (a no-op for resolved content, since last-wins already collapsed it to one).

Uncle Bob audit (engine slice): **Names** `detectIdCollisions` / `mergeContent` / `validatePacks` / `overrides` / `walkStringFields` / `REF_TARGETS` say what they are. **DRY** the collision scan, the pure merge, and the cross-ref walk are each one helper; `resolveContent` = collisions-then-merge, `validatePacks` = collisions + cross-refs, so neither path duplicates the other; the generic ref-walk replaced two hand-written loops. **SRP** `detectIdCollisions` only finds collisions, `mergeContent` only merges, `validateCrossReferences` only resolves refs; `resolveContent` composes the first two and fails fast, `validatePacks` composes for report-all. **Magic numbers** none (the Levenshtein threshold predates this slice). **Mechanical outcomes asserted** within-pack dup throws, undeclared cross-pack collision throws, declared override wins, same id across different categories doesn't collide, `validatePacks` reports collision + dangling ref together without throwing, and the shipped starter pack validates clean. **Tests** each pins a branch of the policy; the contract-snapshot updated for the four new exports (diff is only those symbols). No content change beyond the fixture dedup. No em/en dashes. `tsc --noEmit` clean; full suite green.
